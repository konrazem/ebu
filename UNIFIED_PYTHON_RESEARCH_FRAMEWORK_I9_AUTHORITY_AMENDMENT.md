# Unified Python Research Framework I-9 authority amendment

Status: **prospective, unaudited authority candidate**
Canonical stage: **I-9 — Separately authorized implementation audit**
Accepted predecessor commit: `4ab6f9ca32e32a3801c6a4b6872b34b206e6da7e`
Accepted predecessor tree: `591ad275116e9dc28bf0443aae80142e5ad86ec5`

## 1. Authority and purpose

This amendment freezes only the design authority for a later I-9 implementation-fidelity audit. Its purpose is to determine whether the already integrated I-1 through I-8 framework matches its accepted authorities by using the exact V0 through V11 T0, T1, and T2 validation groups and a closed, non-scientific audit surface. It does not conduct that audit, implement the audit scaffolding, execute a validation group, or make a scientific claim.

The present stage ends when these five coequal candidate documents are mechanically complete and ready for independent authority audit. Independent authority audit has not begun. Later I-9 scaffolding implementation, later V0-V11 execution/reporting, and any scientific execution are separate authority boundaries.

## 2. Governing-source chain and precedence

| Coordinate | Governing source |
|---|---|
| Accepted coordinate | `4ab6f9ca32e32a3801c6a4b6872b34b206e6da7e` / `591ad275116e9dc28bf0443aae80142e5ad86ec5` |
| Stage order and path ownership | `UNIFIED_PYTHON_RESEARCH_FRAMEWORK_IMPLEMENTATION_PLAN.md` §§9, 11, 12 |
| Audit semantics | `UNIFIED_PYTHON_RESEARCH_FRAMEWORK_SPECIFICATION.md` §I-9 |
| Accepted predecessor | I-1 through I-8 authorities, contracts, manifests, repairs, and compatibility amendments |
| Bridge boundary | `SEQUENTIAL_PARALLEL_BRIDGE.md` and accepted I-6 package |
| Dynamic boundary | `DYNAMIC_COORDINATION_FOUNDATION.md` and accepted I-7 package |
| Authorization/bootstrap | accepted I-4 governance/bootstrap and UQ-25 decisions |
| Open records | `POST_ATOMIC_OPEN_PROBLEM_REGISTER.md` and topology programme records |

The exact 73 raw source locks are in `unified_python_research_framework_i9_contract.json`; the predecessor manifest independently locks all 321 accepted tree rows. Precedence is: accepted Git coordinate; exact I-9 plan/specification boundary; accepted I-8 through I-1 authorities and reconciliations; Bridge/Dynamic/governance/UQ dispositions; then roadmap and open-problem records within their still-operative scope. A conflict stops fail-closed. A lower source cannot broaden a higher one.

## 3. Exact package

The package has exactly five files:

1. `UNIFIED_PYTHON_RESEARCH_FRAMEWORK_I9_AUTHORITY_AMENDMENT.md` — normative amendment.
2. `unified_python_research_framework_i9_contract.json` — declarations, ownership, accepted surface, failures, precedence, sources, nonclaims, and stage boundaries.
3. `unified_python_research_framework_i9_validation_contract.json` — closed materialization language, all dynamic/static authority vectors, exact owners, checks, outcomes, FailureIds, and collision audit.
4. `unified_python_research_framework_i9_predecessor_manifest.json` — all 321 predecessor rows and two independent Git reconstruction routes.
5. `unified_python_research_framework_i9_implementation_path_manifest.json` — exact four-path future implementation and compatibility boundary.

No validation report is a sixth package file. A report and immutable provenance may be produced only during a separately authorized later validation execution.

## 4. Accepted surface and I-9 declarations

I-9 preserves the complete accepted I-8 surface: 444 ordered root exports, 280 ordered `FailureCode` members, 154 public signature rows, 39 existing package modules, and every accepted module export. I-9 adds zero public types, zero public callables, zero root exports, zero `FailureCode` members, zero public signatures, and zero hash domains. `errors.py`, `hashing.py`, `identity.py`, `pyproject.toml`, and `requirements-framework.lock` remain byte-identical.

The future `validation` module has `__all__ = ()`. Its only module-level additions are the seven private callables and eight private literal constants frozen in the mechanical contract. It uses `FailureStage.I9` and only accepted failures. FailureId derivation is the accepted `ebu.failure-id.v1` eight-byte big-endian UTF-8 framing and SHA-256 algorithm with the exact private owner interface, empty object references, a typed not-applicable event key, and the first-failure ordinal.

## 5. Validation authority

| Group | Class | Exact test paths |
|---|---:|---|
| V0 | T0 | tests/framework/test_ecj1.py |
| V1 | T0_T1 | tests/framework/test_hash_preimages.py, tests/framework/test_identity_registry.py |
| V2 | T0 | tests/framework/test_numeric.py, tests/framework/test_primitives_envelopes.py |
| V3 | T0 | tests/framework/test_i3_integration.py, tests/framework/test_i3a_declarations.py, tests/framework/test_i3b_declarations.py, tests/framework/test_i3c_declarations.py, tests/framework/test_i3d_declarations.py, tests/framework/test_atomic_declarations.py, tests/framework/test_interaction_declarations.py |
| V4 | T1 | tests/framework/test_authorization.py, tests/framework/test_authorization_use.py |
| V5 | T1 | tests/framework/test_capabilities.py |
| V6 | T0_T1 | tests/framework/test_event_ownership.py |
| V7 | T1 | tests/framework/test_inert_durability.py |
| V8 | T2 | tests/framework/test_bridge_exact_fixtures.py |
| V9 | T2 | tests/framework/test_dynamic_static_identities.py |
| V10 | T1 | tests/framework/test_artifact_recovery_publication.py |
| V11 | T0 | tests/framework/test_route_guards.py, tests/framework/test_validation_reachability.py |

V0-V11 are named here because the controlling I-9 plan requires their explicit authorization. This document still does not execute them. T3 is absent. V8 and V9 remain isolated, hash-bound, one-case T2 fixtures; they do not admit trajectories, successor chaining, causal inference, settlement choice, route science, or schedule comparison.

A later group is a pass only if its exact tests terminate with zero exit, complete a positive number of checks, record source and authority hashes, and report no model, runner, policy, state-advance, network, subprocess, registered-world, result, Gate, or production reachability. Skipped, quarantined, terminated, zero-check, or partially substituted groups fail.

## 6. Complete materialization, ownership, and precedence

The validation contract contains 69 prospective dynamic owner vectors and 28 static witnesses, for 97 total vectors. Each dynamic vector names one construction baseline, a closed `REPLACE`/`DELETE`/`APPEND` mutation program, exact recursive argument materialization, the fully materialized call and canonical identity, one exact private owner call, every ordered predicate, the first active failure, expected result, `FailureCode`, and exact FailureId. Representative-interface substitution and metadata-only evidence are forbidden.

The private owners are exactly: `_validate_group_descriptor`, `_validate_source_locks`, `_validate_implementation_surface`, `_validate_forbidden_reachability`, `_validate_group_evidence`, `_validate_audit_mapping`, and `_authorize_t2_fixture`. The mapping owner requires all 67 specification invariants, 45 specification threats, and 26 implementation-plan threats—138 rows total—in exact order, with one closed disposition and sufficient exact evidence, named blocker, or cited inert outside-I-9 record per row. The T2 owner may delegate once only to the accepted private T2 capability issuer after the exact V8/V9 fixture path, raw hash, case, and interface are validated. No other interface can stand in for an owner.

Owner-local first-failure precedence is frozen in the mechanical and validation contracts. The global later-audit order is predecessor identity; source locks; changed-path closure; dependency/Unicode locks; exports/failures/signatures; import direction/cycles/reachability; V0-V11 evidence in numeric order; threat/invariant/open-item mapping; report/provenance completeness.

## 7. Closed future implementation surface

Only a later, separate I-9 implementation authority may change these paths:

- new `src/ebu_framework/validation.py`;
- new `tests/framework/test_validation_reachability.py`;
- additive guard-only modification of `tests/framework/safety.py`;
- additive CI-only modification of `.github/workflows/tests.yml`.

The future import graph has 40 modules and 250 direct edges with zero cycles. `validation` may directly import only `canonical`, `numeric`, `identity`, `hashing`, `primitives`, `capabilities`, and `errors`; it may not import `execution`. `execution` and every other production module may not import `validation`. Existing framework tests and fixtures remain byte-identical except for the exact additive `safety.py` guard extension; only the new reachability test is added.

CI preserves the existing job, adds push/pull-request T0 and T1 groups, and adds a separately gated `workflow_dispatch` T2 group after exact authority and fixture-hash checks. It contains no T3 job and no runner, finalizer, trajectory, Gate, result, model, policy, production, or network/subprocess entry capable of starting science.

The later audit must obtain fresh installed-distribution/artifact/hash, provider-security/vulnerability, license, and software-bill-of-materials evidence while preserving the exact accepted UQ-25 metadata and lock. Any required provider or lock change is a blocker requiring new prospective dependency authority; I-9 cannot repair it. Production trust bootstrap remains absent and disabled.

## 8. Preserved predecessor semantics

All accepted I-1 through I-8 semantics survive unchanged. In particular: I-8 provenance, immutable-prefix recovery, manifest/publication separation, write-once behavior, and non-destructive correction; I-7 topology-layer provenance, ownership, queue, delay, route, and delayed-effect guards; I-6 M1-M9 physical measurement, same-baseline nonadditivity, comparator relativity, and causal/settlement separation; I-5 event order, durability, trace, policy-memory/physical-state atomicity, and interruption-prefix rules; I-4 external authorization, exact dimensions, durable single-use consumption, UQ-25 dependency pins, and disabled production bootstrap; I-3C settlement-causality repair; and D1/D2 declaration authority and their accepted compatibility reconciliations.

The audit must map every controlling invariant and threat to exact code/evidence or an explicit inert open item. It cannot change behavior to make the map pass.

## 9. Predecessor identity and historical safety

The predecessor manifest reconstructs all 321 paths independently by (A) `git ls-tree` plus `git cat-file` from the exact commit/tree and (B) an in-memory `git archive` member-byte route. Every path, byte count, and raw SHA-256 must agree; route A additionally fixes mode, object type, and blob identity.

The intentionally missing/prunable `/private/tmp/ebu-framework-i3-order-witness-authority` registration and retained index remain exactly untouched. The primary checkout, every I-6/I-7/I-8 authority/implementation/correction worktree, the failed old I-8 worktree, I-8 feature worktree, coupled-feedback/book candidate, and all other historical worktrees remain unentered and unmodified. Unrelated dirty worktrees are inert if unchanged.

## 10. Open records and nonclaims

The mechanical contract retains all 180 open/resolved/superseded/outside disposition-row occurrences from the post-atomic register in document order. In particular, UQ-23/24, UQ-26/27/28/29/30/37/38, DC-25, and CTMP-OP1 through CTMP-OP6 remain inert open or outside-programme records. They are not manufactured I-9 requirements and do not authorize a stochastic engine, real durability/publication backend, institutional correction rule, disclosure regime, composite memory, fault injection, or topology identification. Narrowly resolved rows preserve only their accepted assumptions; superseded rows do not revive.

This package makes no wave, wave-propagation, physical-phase, superposition, interference, or electrical-voltage programme; no causal, settlement, scientific, empirical, institutional, or publication claim beyond accepted authority; no universal topology inference; and no reopening of domain/provider model responsibilities as universal EBU research problems. The accepted ten-phase execution order is preserved as a declarative audit subject, not executed and not conflated with an excluded physical-phase programme.

## 11. Acceptance boundary

Static candidate validation must reproduce strict duplicate-key/float/nonfinite/trailing-data JSON rejection, two independent canonical encoders, raw and canonical identities, all inventories/totals/projections, all vector calls/predicates/outcomes/FailureIds/collisions, all exports/failures/signatures/imports/cycles, path and source closure, Markdown/text/math/link/table integrity, UTF-8/LF/final-LF/no-BOM/no trailing whitespace, repository scope, `git diff --check`, fresh remote target identity, empty index, no residue, and historical-worktree preservation.

Acceptance of this candidate means only that it is ready for independent authority audit. It does not begin that audit, authorize implementation, run V0-V11, produce the later report, authorize science, activate production bootstrap, perform production operations, publish, or authorize any Git integration.

Completion marker: `FRAMEWORK_I9_AUTHORITY_CANDIDATE_READY_FOR_INDEPENDENT_AUDIT`
