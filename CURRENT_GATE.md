# AWS-C0 current gate

- Last implementation commit: `f5d1297bb9e62ad9ea49b87ffe7a12c89ca07bba`; starting SHA for the authorized IAM repair was `27f99190f2be4f75aad0e64b7e2921ad5b26d8cf`. The implementation adds prospective `aws_c0_iam_policy_set_observation/v2` and `aws_c0_runtime_control_read_plan/v3`. The plan reconstructs and validates its exact v2 predecessor and changes only the IAM output kind/schema binding. IAM output/read-plan v1/v2 remain unchanged, and output/v2 requires exact `source_row_ids` R15–R19.
- First remaining blocker: IAM output/v2 still requires `assume_role_policy_sha256`. The mapped R15–R19 operations expose inline/attached policy inventories and documents, but not the role trust policy. The authenticated `AssumeRolePolicyDocument` is exposed by R14 `iam:GetRole`; R14 belongs to `INSTANCE_PROFILE_SOLE_ROLE`, while the plan has `shared_row_ids: []`. The closed mapping and unmapped-control refusal therefore prohibit a truthful IAM producer from silently consuming R14 or substituting an expected hash.
- Exact semantic authority is required for one prospective resolution: explicitly share R14 with `IAM_POLICY_SET`, introduce a versioned authenticated cross-control binding to the R14-backed instance-profile output, or version the IAM output again to change/remove the trust-policy field. None is authorized by the prior row-ID-only amendment, so no option has begun.
- Authority remains one existing `t3.small` platform smoke only under the aggregate USD50 cap; R64 permits at most three exact guarded `ec2:DescribeSecurityGroups` reads. Stage F scientific/model/simulation execution, larger compute, push, and publication remain excluded. No AWS call, credential use, IAM change, resource mutation, model state, or scientific experiment occurred.
- Evidence: focused IAM/read-plan tests pass (3/3); the committed-byte deployment-manifest test passes; deterministic schema generation and `git diff --check` pass. The post-commit complete offline suite ran 233 tests in 140.878 seconds: 232 passed, one errored, zero assertion failures, skips, or expected failures. The sole error remains the deliberately unsatisfied complete reconstruction-set public gate. Full-suite log `/private/tmp/ebu-aws-c0-post-iam-v2-committed-full-suite.log` SHA-256 `a9e2d0351cfff7123b49e09e6f4082a4a85f3110ab3591b44876bb4c411e0d78`; producer-source conflict record `/private/tmp/ebu-aws-c0-iam-v2-producer-source-conflict.json` SHA-256 `08bb0812a6dfa00eb9eba2a310e7f1cbbd523ba2ce605080b9681e3029271605`; generated evidence-schema SHA-256 `e9534c06f189d6258343637b16d7003018962b3af9bc58272a2551acf216a720`; read-plan/v3 identity SHA-256 `c9d2abf122a8b48828ce40fe4b8beb1346c7e7c19bfc85a1268d1747bb3d3bf7`.
- The source-bound attachment remains `PARTIAL_SOURCE_BOUND_NOT_READY` with seven unresolved controls. No independent-review claim is made because the active instruction forbids creating an assistant. AWS preflight and smoke have not begun.
