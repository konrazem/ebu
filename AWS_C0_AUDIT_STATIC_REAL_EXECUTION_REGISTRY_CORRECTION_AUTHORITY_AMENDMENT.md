# AWS-C0 audit/static real-execution registry correction authority

Status: **PROSPECTIVE AUTHORITY ONLY — NO IMPLEMENTATION, AWS, DEPLOYMENT,
CONTAINER, NETWORK, OR SCIENTIFIC EXECUTION AUTHORITY**.

Authority identifier:
`EBU-AWS-C0-AUDIT-STATIC-REAL-EXECUTION-REGISTRY-CORRECTION-AUTHORITY-v1`.

The exact accepted base is commit
`3b185d9f770e525df645a2c6d2eda678e28554ef`, tree
`aa7edcfcba250d6954de8c9ff42151cd7fb77ff3`. This amendment adds exactly
the six mode-`100644` authority paths named by its implementation-path
manifest. It predicts no future commit, tree, blob, S3 VersionId, receipt, or
root bytes and authorizes no commit, merge, push, AWS call, Docker call,
network call, package installation, Stage-F activation, or scientific work.

## 1. Narrow defect and correction

The accepted audit/static handoff authority correctly closed the acyclic
publication chronology, but its prospective static-v3 execution model allowed
the later implementation to collapse unrelated validation coordinates into a
small generic semantic model. Such a model can manufacture the expected
disposition and code without running the declared constructor and real
validator predicate. Its `352` count is the number of parent case coordinates,
not the number of real validation axes. Its `104` count omits direct cases and
required split axes. Neither number proves complete real execution.

This authority replaces the never-emitted AUDIT-category root with
`aws_c0_audit_static_handoff_authority_audit/v4` and the never-emitted
STATIC-category root with
`aws_c0_material_runtime_static_validation/v4`. No audit-v3 or static-v3 bytes
exist as accepted evidence. Audit-v4 must not invent, embed, predict, or require
such bytes. It embeds and reconstructs the accepted material audit-v2, binds
the accepted handoff authority and reachability chain, binds this correction's
future authority and reachability chain, and finally binds the future exact
fourteen-path implementation candidate and integration.

The following programme boundaries remain exact:

- 21 pre-live objects;
- 12 semantic evidence-root categories;
- 22 cost dimensions;
- 63 sealed read-plan rows;
- 14 implementation paths;
- ten zero-science counters;
- Stage E accepted and finished without rerun;
- Stage F frozen behind separate Linux binding, scientific packet, validation,
  and explicit execution authority; and
- zero model, simulation, trajectory, Gate, scientific RNG, outcome inspection,
  interpretation, or publication work.

## 2. Six bound files, five active case groups

Static-v4 binds exact file identities for six validation contracts, in order:

1. original AWS-C0, 66 parent cases and 66 real axes;
2. live-preparation correction, 84 parent cases and 99 real axes;
3. cost/runtime/retrieval correction, 108 parent cases and 222 real axes;
4. material/runtime correction, 66 parent cases and 197 real axes;
5. the accepted audit/static handoff contract, retained only as superseded
   authority history; and
6. this real-execution-registry correction contract, 28 parent cases and 189
   real axes.

There are exactly five active case groups: entries 1, 2, 3, 4, and 6. Entry 5
is not a sixth active group and its cases are not executed beside their
replacement. Static-v4 carries three closed lists: six bound contract-file
identities, five active group identities, and one explicit supersession row.
The active parent arithmetic remains
`66 + 84 + 108 + 66 + 28 = 352`. The real-axis arithmetic is
`66 + 99 + 222 + 197 + 189 = 773`.

The preserved expansions are exact: live preparation is `84 - 3 + 18 = 99`,
cost/runtime/retrieval is `108 - 17 + 131 = 222`, and material/runtime is the
literal 197-axis registry.

The 28 top-level `C0-ASPH-P01` through `P08` and `C0-ASPH-N01` through `N20`
identifiers remain the fifth group's parent identifiers. Their active subcase
registry is versioned and replaces the old registry. Old subcase identifiers
are historical only and cannot satisfy an active receipt.

Preservation includes meaning and expected code, not merely spelling. In
particular P04 remains the one `ALL_352_CASES_PASS` execution, P06 remains the
exclusive atomic-output execution, N01/N02/N03 remain unknown/duplicate/
missing-argument refusals, N06 remains stdin refusal, N08/N09 remain the two
source-HEAD integration checks, and N13 remains missing-provisional-observation
refusal with no S3-latest inference. New correction-meta gates do not steal
those coordinates and are validated outside the 189 active ASPH axes.

## 3. Exact corrected ASPH arithmetic

The replacement fifth group contains exactly 180 independent subcases and nine
direct cases, hence 189 real axes. Positive axes total 23 and negative axes
total 166. Across all five active groups, positive axes total 167 and negative
axes total 606.

Positive per-case counts are exactly:

```text
P01=3 P02=4 P03=3 P04=1-direct P05=3 P06=1-direct P07=5 P08=3
```

Negative per-case counts are exactly:

```text
N01=1-direct N02=1-direct N03=1-direct N04=5 N05=7
N06=1-direct N07=6 N08=1-direct N09=1-direct N10=2
N11=11 N12=8 N13=1-direct N14=50 N15=5 N16=9 N17=5
N18=9 N19=4 N20=38
```

The nine direct cases are P04, P06, N01, N02, N03, N06, N08, N09, and N13.
All other fifth-group cases aggregate their exact ordered subcase receipts.
Static-v4 contains exactly 352 parent case receipts, a separate exact array of
773 real-axis receipts, and exactly 849 member-execution receipts obtained by
prefix-ordered summation of every registry row's `members_in_order`. It never
describes 352 as the real-execution count or 773 as the member-execution count.

## 4. Closed real-execution registry

The registry identity kind is `aws_c0_static_real_execution_registry/v1`, the
fixture envelope is `aws_c0_static_case_fixtures/v4`, and an atomic receipt uses
`aws_c0_validation_axis_execution_receipt/v1`. Every registry row binds its
versioned axis ID, semantic name, parent case, active contract identity and
path, polarity, direct/subcase class, ordered members, operator, distinct
constructor callable, real validator callable, selector, fixture pointer and
hash, predicate identity and source hash, base error class, expected
disposition and code, and a closed propagation proof.

The axis namespace is not globally ASPH-specific. The registry binds five
complete deterministic derivation tables in the frozen group order. They
produce 66, 99, 222, 197, and 189 ordered IDs respectively; only the fifth
table uses the `C0-ASPH-V2::<parent>::<versioned semantic name>` formula. The
registry carries a unique 773-ID order, a 773-key map, and 773 ordered rows.
All three views must agree on every ID, contract, group, parent, row identity,
and the `66+99+222+197+189` partition. Summing each row's exact ordered member
set produces exactly 849 member-execution coordinates: group totals
`66+99+222+197+265` and multiplicities 735 singleton plus 38 triple-member
axes.

Every active axis executes exactly once. A positive axis invokes every frozen
member and records an ordered member-receipt aggregate. A negative axis mutates
one frozen named member or field and invokes the real validator. The observed
disposition, code, and error class come only from the callable return or caught
typed refusal. Copying an expected value into an observed field, generic lane
dispatch, marker-only PASS, or an unexecuted coordinate refuses.

Each atomic receipt binds the registry-entry identity, active contract and
parent, operator, constructor and validator identities, fixture hash,
before/after hashes, invocation count one, actual observed disposition/code/
error class, ordered member receipts and aggregate, and its own closed digest.
Each parent receipt aggregates only its exact ordered axes. Static-v4 separately
aggregates all 352 parent receipts, all 773 atomic receipts, and all 849 exact
member receipts nested in their registry order.

Counts alone are insufficient. Static-v4 also binds unique ordered ID lists,
keyed receipt maps, and ordered receipt arrays for all 352 parents and all 773
axes. A recomputed cross-binding gate proves equality among list IDs, map keys,
embedded row IDs, registry rows, group partitions, aggregates, and the five
active contract identities, and proves that no receipt belongs to the
superseded ASPH contract.

Every axis additionally binds parsed source bytes, constructor and validator
AST pointers and hashes, and real call-graph edges. The audit rejects an
expected literal read, expected-to-observed dataflow, axis/kind/schema/selector/
code switch used as a generic dispatcher, an absent call edge, or a callable
whose source identity does not match the final implementation tree. Unique
wrapper names around one generic lane therefore do not pass.

## 5. Equivalence-class soundness

One axis may cover multiple members only when the member set is explicit,
closed, and ordered; the validator predicate is identical and branchless for
every member; the base error class and validator source hash are identical; a
positive execution invokes every member; a negative execution mutates one
frozen named member or field; and an explicit propagation proof covers every
member.

Any kind, branch, schema, selector, expected code, base error class, or source
hash difference requires a separate axis. A generic dispatcher, open or
computed member set, representative-only run, marker-only PASS, unexecuted
member, or propagation assertion without member receipts refuses. There is no
representative loophole.

## 6. Constructible split-preimage refusal

`N20_SPLIT_PREIMAGE_TRANSPORT_PAIR` replaces the nonconstructible fixed-point
test. Its outer Base64 is the strict Base64 of canonical semantic preimage A,
while its outer SHA-256 is the lowercase SHA-256 of a different canonical
semantic preimage B. The fixture proves `A != B`. The validator decodes the
Base64, requires those decoded bytes to equal the one declared semantic request
preimage, recomputes SHA-256 over those same bytes, and compares it with the
outer SHA field. It returns
`SSM_TRANSPORT_PAIR_DERIVATION_MISMATCH`. Neither transport field occurs inside
the semantic preimage, and no fixed point is sought or claimed.

## 7. Controller prepare-request provenance

The controller `prepare-request` entry point obtains every semantic21 value
only from its frozen explicit argv flags. It performs exactly three declared
exact-VersionId S3 GETs, in order, for launch-v4, live-packet-v4, and
live-authorization-v4. Each GET coordinate—bucket, key, VersionId, expected
byte count, and expected SHA-256—comes only from those flags. Each response is
SigV4/TLS captured, byte-counted, hashed, schema-validated, identity-validated,
and cross-bound to the requested coordinate. There is no other semantic-source
network call. Retrieved content cannot replace or supply a semantic parameter.

Those three authenticated GETs require temporary instance-profile credentials.
The only additional controller network channel is a bounded, nonsemantic
IMDSv2 bootstrap: one link-local token PUT with an explicit TTL header, one
role-name GET with the token header, and one credential-document GET for that
exact role name with the same token header. Proxying and redirects refuse;
other metadata or credential sources refuse; SSM carries no credential bytes.
Preparation derives a closed sealed EC2 role context from the accepted
R02/R03/R13/R14 instance-profile/sole-role reconstruction and authenticated
R14 role source: account ID, role ID/name/ARN, instance-profile ARN, fixed
instance ID, and private-snapshot identity/hash. Launch-v4 carries its identity,
complete canonical preimage, and hash binding, so the controller needs no
fourth GET and no runtime STS call. It deterministically derives the actual
session principal as `<role-id>:<instance-id>` and the assumed-role ARN as
`arn:aws:sts::<account-id>:assumed-role/<role-name>/<instance-id>`.
The CloudFormation and read-only preflight evidence bind `HttpTokens=required`,
the enabled IPv4 metadata endpoint, hop limit one or two, disabled metadata
IPv6, and the fixed IMDS/S3 network allowance. Token, role, credential-document,
role-session and expiry fields are mechanically cross-bound. The normalized
role-name response bytes determine their UTF-8 byte count and SHA-256, bind the
explicit role-name fields inside the complete closed approved-role preimage,
and equal the credential-GET path suffix. The field-extraction, approved-role,
approved-role-session, credential-source, and imported launch-sealed EC2
role-context identities each set both identity digest fields to SHA-256 of the
exact RFC 8785 canonical complete preimage;
no identity digest is equated to a role-name, session-ARN, or source-document
leaf hash. Those leaf hashes bind only to explicit projected preimage fields.
The token response/request byte counts and extracted access-key, secret, and
session-token byte counts are likewise executed equality receipts. Only hashes
and byte counts of secrets persist.

Environment, undeclared-file, metadata, arbitrary socket, stdin, or network-
response values used as alternate semantic-parameter sources refuse. This
narrow source-provenance rule does not forbid the three declared authenticated
content retrievals required to create and verify the source files.

The resulting root-owned exclusive `0600` nonsymlink regular sidecar binds the
exact 21-field semantic request, the two-field transport pair, all three closed
versioned source preimages and actual GET request/response/SigV4/TLS captures,
the IMDSv2 bootstrap, the metadata/network attestation, and the predeclared
capture obligation. It does not embed a provenance proof that already contains
its own identity. After the sidecar identity exists, runtime-start-bundle-v2
binds its complete canonical bytes and the complete provenance proof;
start-receipt-v5 persists the bundle, sidecar, and three-GET aggregate
identities. The semantic preimage excludes only the two transport fields. The
actual SendCommand parameter map is semantic21 plus transport2. Missing either
transport field, missing any semantic field, duplicates, unknowns, a split
derivation, range failure, undeclared retrieval, or inherited-source mismatch
refuses.

## 8. Achievable SigV4, TLS, expiry, and zeroization evidence

PutObject, exact-version GetObject, and every ListObjectVersions page use the
actual credentials for one of three closed authorization contexts: preparation
operator, live EC2 instance profile, or closure Lambda execution role. EC2 uses
only the bounded IMDSv2 bootstrap above. Lambda uses the standard runtime
environment fields `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, and
`AWS_SESSION_TOKEN`; an ECS-style container endpoint is not authorized, and it
does not claim EC2 or preparation credentials. Standard Lambda runtime
credentials do not expose their exact expiry. The Lambda proof therefore binds
a null expiry and explicitly refuses an exact-expiry claim. It instead binds
the invocation start, deadline, timeout of at most 900 seconds, request and
completion inside that bound, and an authenticated response without an expiry
or signature rejection. Preparation and EC2 retain exact source expiry. At
request time the workflow can
record the exact algorithm and credential scope, both equal signed-header
views including `x-amz-security-token`, canonical nonsecret headers, access-key
ID hash and byte count, and the AccessKeyId parsed from Authorization
Credential,
token hash and byte count, operation-owner authorization context, approved
role/session/policy/credential-source/preparation-authorization identities,
requested and completed UTC, context-specific expiry evidence and
accounting bounds, canonical-request/string-to-sign/payload hashes,
Authorization signature, and runtime recomputation success. Each capture binds
a closed nonsecret transcript identity, complete canonical preimage bytes,
byte count, SHA-256, canonical-request and string-to-sign bytes/hashes, scope,
time, credential-copy identity, observed/local signature equality, and literal
cross-field execution receipts. The authenticated-operation capture also binds
a typed AccessKeyId-extraction identity and complete canonical preimage. Its
exact eleven executed receipts decode and hash the captured request envelope,
require exactly one case-insensitive `Authorization` header at
`/headers/authorization`, parse the `AWS4-HMAC-SHA256 Credential=` value,
recompute the extracted AccessKeyId UTF-8 bytes, byte count, and SHA-256, and
parse the credential scope. Those extracted bytes, count, and digest equal the
SigV4 transcript, selected authorization-context source, and application-owned
per-operation credential copy through the authenticated-operation capture's
exact 23 ordered preimage equalities. A claimed scalar without this
request-envelope extraction refuses. Each per-operation credential-copy identity is
hash-bound to its complete canonical preimage, and the selected credential
source, copy, and Authorization Credential mechanically agree on access-key ID,
authorized secret, and token hashes and byte counts. Raw tokens, access secrets, and secret-derived
keys are never persisted. Zeroization is limited to a best-effort proof after
each operation's application-owned credential copy reaches its own last use: the
application's own mutable token, secret, date-key, region-key, service-key,
and signing-key buffers are overwritten in place with zero bytes, their
post-overwrite zero-byte hashes are recorded, and application references are
released. The record explicitly does not claim erasure of Python immutable
copies or SDK, TLS-library, operating-system, or other external copies.
SigV4, TLS, and complete authenticated-operation captures each use a distinct
domain-separated projected-body hash. Their exact canonical body bytes exclude
only their enumerated body/Base64/count/hash fields, so the external typed
identity can bind `sha256` and `value` without a self-hash.

The artifact bucket is lowercase DNS/TLS-safe and contains no dot. Virtual-host
style is mandatory. The response records the expected virtual host, signed
Host, canonical Host, TLS server name, TLS
protocol, peer-certificate SHA-256, hostname and chain verification results,
response-header SHA-256, and request ID. Seven executed derivations construct
the virtual host from the no-dot bucket name, extract exactly one lowercase
Host value from the canonical SigV4 headers, extract `x-amz-request-id` from
the authenticated response, and bind those values through signed Host,
canonical Host, TLS server name, and both request-ID fields. Duplicated local
TLS fields without these source-byte derivations do not pass. Content length, checksum mode, checksum, dates, and conditional
headers cross-bind where applicable. Later offline validation rechecks this
closed runtime attestation and the authenticated exact-version response; it
does not claim to rederive a signature after the secret is gone. No required
field depends on a secret after runtime recomputation and the scoped owned-
buffer wipe and reference release.

The preparation operator remains evidenced by the accepted provisional Put
and authenticated readback observation contract, preserving N14; it is not
inserted into either later runtime journal. Every substantive authenticated S3
PutObject, exact-version GetObject, and ListObjectVersions page owned by the
live EC2 instance profile or closure Lambda execution role is recorded as a
complete canonical capture envelope. The controller keeps a root-owned bounded
hash-chained journal through the
terminal Put response and publishes it once, conditionally without replacement,
at the exact controller-journal key. The finalizer keeps a second bounded
hash-chained journal through the final-manifest publication Put response and
publishes it once at the exact finalizer-journal key. The two journal Puts and
their two exact-VersionId readbacks are the only operations excluded from
recursive capture; they remain bound by exact key, VersionId, ETag, checksum,
byte count, SHA-256, and object receipt. Controller and finalizer keys have
literal distinct suffixes, are mechanically unequal, and the final aggregate
executes exact role-specific coordinate equalities against both corresponding
publication and readback preimages. Any fifth exception refuses.

Envelope hashing is acyclic. The body hash covers exact RFC 8785 canonical body
bytes with only the enumerated derived body/hash fields removed. Each chain
link hashes a domain separator, int64 sequence, prior link (or fixed zero
genesis), and body hash. The journal embeds only the final content-chain hash;
the complete published-object SHA-256 exists only in the external publication
and readback receipts. No full-object fixed point is claimed.

Before live AWS activity, static-v4 binds a two-producer capture-budget
capability over exact controller and finalizer source bytes, AST/call graph,
schedules, fixed operations, bounded list pages, and finite per-category
canonical envelope maxima. Each producer binds complete closed AST and call-
graph proof preimages and identities. Every scheduled, fixed, list, and
reserved-failure category has exactly one ordered complete canonical maximum-
witness preimage and identity; its declared maximum equals the exact witness
byte count or a recomputed bounded parameterized formula. Launch-v4 binds the separate evaluation with actual
frozen durations and intervals. For each producer it derives scheduled, fixed,
page-expanded, reserved-failure, total-count, and total-byte arithmetic. Zero
interval means inapplicable with zero multiplicity and count; otherwise the
interval and multiplicity are positive with an exact first offset and boundary
rule. A positive interval still derives zero captures when the frozen duration
ends before that first offset. Exactly one finite failure envelope is included
in both sums. Launch
refuses before live AWS unless count is at most 512 and canonical journal bytes
are at most 1,048,576 for each producer. At runtime each substantive request
reserves that failure envelope or refuses before sending the request.

The capability body expected identity value equals the capability body hash and
the `sha256` and `value` of static-v4's paired
`journal_capture_budget_capability_identity`. The evaluation body expected
identity value likewise equals the evaluation body hash and the `sha256` and
`value` of launch-v4's paired `journal_capture_budget_evaluation_identity`; it
never equals the enclosing launch-request root identity. The evaluation also
binds the capability identity byte-for-byte to static-v4's paired capability
identity.

The closure Lambda reads both journal versions, validates their complete
sequence, counts, hash chains, publication/readback receipts, and absence of a
later substantive S3 operation, then returns a fixed-size non-S3
closure-response-v3 aggregate. Step Functions does not carry full capture
history, the closure response does not embed it, and the immutable journal
objects remain the only full-history carrier. This finite channel uses the
already authorized controller/finalizer/CloudFormation paths and adds no
fifteenth implementation path.

The two journal objects are closed nonsemantic capture-channel auxiliaries.
Only their two exact keys are excluded from semantic retrieval counts and they
are validated separately by closure. They do not alter the frozen 21 pre-live
objects, 12 semantic root categories, 63 sealed read-plan rows, 352 parent
receipts, or 773 real-axis receipts; any other auxiliary key or count exclusion
refuses.

N14 therefore has exactly 50 independently executable SigV4/TLS/credential
axes. It separates Put/Get/List security-token omissions; raw token, access
secret, and derived-key persistence; token hash and length; signed-header and
canonical-header views; role/session/policy/source/authorization identities;
caller-session existence; request/completion/expiry/accounting/time ordering;
all credential-scope components; signature, dates and hashes; runtime
recomputation; zeroization; offline overclaim; three Host views; content length
and checksum mode; and all TLS/certificate/response/request-ID facts.
The five additive N14 axes are
`N14_ACCESS_KEY_ID_SOURCE_CROSSBIND_MISMATCH`,
`N14_ACCESS_SECRET_SOURCE_COPY_CROSSBIND_MISMATCH`,
`N14_CREDENTIAL_COPY_IDENTITY_PREIMAGE_MISMATCH`, the singleton
`N14_LAMBDA_CREDENTIAL_VALIDITY_OVERCLAIM`, and
`N14_ACCESS_KEY_ID_EXTRACTION_IDENTITY_PREIMAGE_MISMATCH`. The first three and
the fifth execute exact Put/Get/List members; the fourth executes
`CLOSURE_LAMBDA_CREDENTIAL_VALIDITY`. The extraction-identity axis is separate
from source cross-binding because it invokes the typed extraction
identity/preimage validator over the captured Authorization header; a claimed
AccessKeyId scalar cannot substitute for that execution. The complete registry
therefore binds 849 member-execution coordinates while retaining 773
independent axes.

## 9. Audit-v4 and static-v4

Audit-v4 contains the full common authority, record-class, Stage/science false,
ten zero-counter, deterministic-time, and root-digest fields. It embeds and
validates the complete accepted material audit-v2 canonical bytes. The
embedding binds the accepted schema path, mode, blob, byte count, SHA-256 and
`#/$defs/authority_audit_v2` pointer; exact v2 kind and decoded schema; byte
count, raw and decoded root hashes, observed UTC; canonical encoding; root
identity recomputation; final-source reconstruction; and zero separate
publications. A generic identity plus an unchecked Base64 string does not pass.

Audit-v4 binds, in order, path groups
`6,6,1,1,6,6,1,1,14,14` for handoff authority candidate/integration,
handoff reachability candidate/integration, this correction's authority
candidate/integration, this correction's reachability candidate/integration,
and final implementation candidate/integration. Every coordinate carries the
exact commit, tree, complete parent array, literal path, mode, blob, SHA-256,
byte count, and ancestry proof. The arrays use exact prefix-ordered literal
paths with no duplicates, and candidate/integration parent cardinalities are
exactly one/two throughout. Audit-v4 contains no publication coordinate or
observation.

Static-v4 binds:

- audit-v4 and its closed v2 input observation;
- the complete audit provisional conditional-Put observation bytes and
  decoded coordinate, validated against the accepted generic observation shape
  plus the audit-v4 record-kind constraint, with its own PASS gate;
- six contract-file identities and five active group identities;
- one old-ASPH-to-new-registry supersession row;
- validator software-v4 and configuration-v4;
- unchanged output-protocol-v3;
- the complete registry identity and preimage;
- complete five-group axis derivation tables, 352/773 unique order/map/row
  cross-bindings, the exact 849-member ordered sum and receipts, and the
  anti-generic AST/call-graph proof;
- the controller source/AST capability proof, exact three-GET plan, closed
  SigV4/TLS capture schemas, credential-context profiles, and two-journal
  obligation, but no not-yet-observed runtime capture bytes;
- the exact two-producer journal capture-budget capability, with no future
  launch values;
- the immutable outer-record upgrade catalog and separate 18-receipt execution
  proof described below;
- exact 14 implementation rows and scopes 162/176/112/126;
- exactly 352 parent, 773 atomic-axis, and 849 member-execution receipts with
  separate exact order and aggregate checks;
- exact `180 + 9 = 189` fifth-group arithmetic; and
- all source, ancestry, path, audit, registry, case, output, equivalence, and
  zero-science PASS gates.

Static-v4 contains no observation for its own future publication.

For every applicable scheduled capture category, the launch evaluator executes
the exact integral-second boundary rule. When `end_boundary_included` is true,
the count is zero if duration is less than the first offset and otherwise is
`1 + floor((duration - first_offset) / interval)`. When it is false, the count
is zero if duration is less than or equal to the first offset and otherwise is
`1 + floor((duration - first_offset - 1) / interval)`. A non-applicable
category has zero interval, multiplicity, offset, and count; no validator may
ignore or infer the boundary boolean.

## 10. Exact outer-record version upgrades

The accepted closed schemas do not admit silent kind substitution or companion
evidence. Preparation-packet-v2 has an exact old planned-kind array;
preparation closure, live packet, launch, retrieval, and final manifest cannot
literally embed the four publication-upgrade objects within their closed
records.

The upgrade authority is acyclicly split. The immutable
`aws_c0_outer_record_upgrade_catalog/v1` contains only accepted C/M/H schema
coordinates, OB00–OB09 and AR00–AR07 definitions, field maps, literal
additions, and its explicitly projected body hash. A separate
`aws_c0_outer_record_upgrade_execution_proof/v1` binds the already-derived
catalog identity and exact ten OB plus eight AR synthetic execution receipts
and aggregates. Every row carries an immutable complete receipt preimage and
an identity whose `sha256` and `value` are recomputed from the RFC 8785
canonical complete preimage; there is no self-referential row-proof hash. The
aggregate consumes those identities and preimages directly in OB00–OB09 then
AR00–AR07 order, with no duplicate generic identity array. Static-v4
binds the catalog and execution-proof body expected identity values to the
`sha256` and `value` of their respective paired static-v4 embedded identities,
never to the static-v4 root identity. Because static-v4
embeds this synthetic execution proof, the complete local fixtures in OB02,
OB03, OB08, OB09, AR04, and AR07 use exact prefix-ordered independent fixture
static-v4 identity coordinates. Those coordinates prove only the internal
equalities of their own complete synthetic fixture, set current-enclosing-
static cross-binding to false, and never claim the bytes or identity of the
static-v4 record that contains the proof. Prefix-ordered extraction receipts
bind every direct or Base64-decoded static identity to exact fixture bytes.
The exact authenticated-readback complete-record byte pointers are separately
enumerated and explicitly are projection-fixture bytes, not a preimage claim
for the enclosing current static-v4. This rule also covers transitive static
identities nested in publication-upgrade-v4. AR04 fixture mode has a
null post-publication observation. Actual later runtime records, including real
closure-response-v3, bind the already-published current static-v4 through an
authenticated publication observation outside the static-embedded proof, plus
the catalog identity and independent OB/AR aggregates.
Several accepted schemas have closed shapes; verified-object excludes the new
root kinds; and start-v4 and
safe-close-v2 hard-code dispatch-v1. Therefore this authority requires exact
version bumps rather than asserting compatibility.

The ten outer-binding rows are, in order:

1. `OB00_PREPARATION_PACKET_DIRECT_PLAN`: preparation-packet-v3, with the
   exact 21-kind list containing packet-v3, audit-v4, static-v4, launch-v4,
   closure-v3, and live-packet-v4;
2. `OB01_PREPARATION_AUTHORIZATION_PACKET_BIND`: preparation-authorization-v2
   unchanged in shape, with its generic packet identity constrained by this
   authority to preparation-packet-v3;
3. `OB02_LAUNCH_DIRECT_ROOT_PUBLICATION`: launch-request-v4, binding packet-v3,
   the direct v4 roots and objects, literal publication-upgrade-v4, the sealed
   EC2 role-context identity and complete preimage, and the complete pre-live
   two-producer journal capture-budget evaluation;
4. `OB03_PREPARATION_CLOSURE_DIRECT_ROOT_PUBLICATION`: preparation-closure-v3,
   with direct audit/static v4 roots and objects plus literal publication-
   upgrade-v4;
5. `OB04_LIVE_PACKET_DIRECT_ROOT_PUBLICATION`: live-packet-v4, binding
   closure-v3 and launch-v4 and literal publication-upgrade-v4;
6. `OB05_LIVE_AUTHORIZATION_TRANSITIVE_RECONSTRUCTION`: live-authorization-v4,
   binding live-packet-v4;
7. `OB06_START_TRANSITIVE_RECONSTRUCTION`: start-receipt-v5, binding launch-v4,
   live-packet-v4, live-authorization-v4, dispatch-v2, and runtime-start-
   bundle-v2;
8. `OB07_SAFE_CLOSE_TRANSITIVE_RECONSTRUCTION`: safe-close-v3, binding start-v5,
   dispatch-v2, and SSM-completion-v2;
9. `OB08_RETRIEVAL_DIRECT_ROOT_PUBLICATION`: retrieval-verification-v4, with verified-object-v2, root zero audit-v4,
   root one static-v4, pre-live slot three launch-v4, and literal publication-
   upgrade-v4; and
10. `OB09_FINAL_DIRECT_ROOT_PUBLICATION`: final-manifest-v4, binding the bumped roots, launch/start/safe/retrieval
    chain, ordered identities, and literal publication-upgrade-v4.

The forced ancillary definitions are source-sidecar-v3, runtime-start-
attestation-bundle-v2, SSM-completion-observation-v2, final-manifest-
publication-observation-v2, closure-response-v3, verified-object-v2,
SSM-dispatch-request-attestation-v2, and publication-upgrade-binding-v4.
Publication-upgrade-binding-v4 uses local specialized v4 provisional-Put and
authenticated-readback observation overlays. The AR07 accepted projection
rewrites exactly the audit/static root identity kinds and the two provisional
observation `record_identity.kind` values; the accepted generic readback shape
needs no byte rewrite, while the new v4 schema constrains both readback
`record_identity` values to their v4 roots. A specialized accepted v3
observation may not be reused as if it observed a v4 root.
Source-sidecar-v3 carries the three actual GET captures and credential/network
attestations; runtime-start-bundle-v2 carries the later, non-self-referential
provenance proof; closure-response-v3 carries the fixed-size final journal
aggregate and the ten-row/eight-row projection-revalidation aggregates.
Preparation-authorization stays v2 and the semantic21-plus-transport2 SSM
dispatch request stays v2. Dispatch-v2 is transport, not a direct AUDIT/STATIC
root-embedding context.

The evidence schema freezes the accepted C/M/H schema file coordinates and an
exact old-to-new field map for every bump. Ten executable `OB00`–`OB09`
projection receipts and eight executable `AR00`–`AR07` ancillary receipts bind
the complete new canonical local projection-fixture record and schema bytes,
without claiming to contain a future AWS runtime record, and bind the accepted source/pointer,
field map, literal additions, root/object/predecessor equalities, accepted
projection validation, new-schema validation, and recomputed identity. Separate
ten-row and eight-row aggregates are mandatory. Validation rejects unknown keys,
removes only the named literal additions, rewrites only the named new values to
their accepted predecessor values, validates the resulting projection against
the exact accepted pointer, reapplies every new-kind and added-field rule, and
recomputes the complete canonical identity. Any unlisted version change or an
old v3 root in a new record refuses.

C is blob `ad4e8312a6833f03b2ca90d9bcb19830083ae270`, 102327 bytes,
SHA-256 `271d2c44835616334aeae6e781b52466977730ee17a8c295fb47b09a6ffecb29`;
M is blob `fe9a08b2e93a9e50094c57b3b6d41f3b82082098`, 769841 bytes,
SHA-256 `c8edb270401e0edd740a4eb4fa6da0c10410d28c0b891e0c38b03148c097fe99`;
and H is blob `65e05f9085af23c455954df43de68b95a51c85d2`, 113583
bytes, SHA-256
`915669f809d3a1727a933f5e91cdfded8de2bcc09e20205d7da075d040d5c763`.

## 11. Deterministic CLI and publication chronology

The public local audit/static CLI retains exactly two modes and argv schemas.
That local validator reads no ambient environment, stdin, socket, metadata
endpoint, credential provider, network, or undeclared file. This restriction
does not erase the later controller's declared three exact S3 GETs, bounded
nonsemantic IMDSv2 bootstrap, or the post-run two-journal closure validation.
Output remains absolute, normalized, exclusive, atomic,
canonical UTF-8 NFC JSON with no final LF and no partial file.

Mode A emits audit-v4 only after the final implementation integration exists.
The operator then conditionally puts those exact bytes. Mode B consumes the
exact local audit-v4 bytes and its provisional Put observation, executes all
five active groups, all 773 axes, and all 849 member executions required by
those axes, and emits static-v4. The operator
conditionally puts static-v4 and independently reads both roots by exact
VersionId with authenticated complete bounded version-history evidence.
Preparation closure and live packet bind both provisional and authenticated
observations. No root predicts or contains its own future publication.

Before any live AWS action, launch-v4 binds the complete capture-budget
evaluation to the static-v4 capability and refuses if either producer's exact
count or canonical-byte inequality fails.

After static publication, source-sidecar-v3 records the three actual controller
GET captures; runtime-start-bundle-v2 and start-v5 bind them forward. The
three declared Gets each embed a typed authenticated-operation capture and
execute 25 closed checks: Base64 decoding and hashes for request and response;
request/response identity `sha256` and `value`; object bytes and
expected/observed count and hash; source-role, bucket, key, and VersionId fields
in request and response; SigV4 and TLS identity/preimage hashes; and exact
equality with the nested complete operation capture; source-role to controller
owner; bucket equality across request, SigV4, and TLS; and exact key/version
extraction from the canonical URI/query. Parallel identity or
Base64 fields without those executed equalities do not pass. The
controller and finalizer subsequently seal and publish their two capture
journals. Only after the finalizer journal is published does closure read both
exact versions, verify the complete capture sequence and projection aggregates,
and return closure-response-v3. Retrieval-v4 and final-v4 bind the earlier
capture obligation only; neither fabricates the later journal aggregate.

## 12. Future path and reachability scope

After these six files are separately accepted and integrated, reachability may
modify only `tests/framework/test_validation_reachability.py`, add no path, and
introduce exactly:

- `AWS_C0_AUDIT_STATIC_REAL_EXECUTION_REGISTRY_CORRECTION_AUTHORITY_ONLY`; and
- `AWS_C0_AUDIT_STATIC_REAL_EXECUTION_REGISTRY_CORRECTION_CLOSED_IMPLEMENTATION`.

The scopes are exactly 162/176 globally and 112/126 in the descendant scope.
The later implementation adds the same exact fourteen mode-`100644` paths from
the accepted manifest. No fifteenth registry path is needed: the registry and
fixtures live in the already authorized validator and fixture paths. Globs,
ignore-based discovery, extra paths, copied diagnostic bytes, or a different
base refuses.

Completion marker:
`AWS_C0_AUDIT_STATIC_REAL_EXECUTION_REGISTRY_CORRECTION_AUTHORITY_COMPLETE`.
