# Canonical Topology / Motif Programme Implementation Staging Plan

**Status:** Prospective documentation-only staging authority; no implementation begun
**Version:** 1.0.0
**Predecessor:** framework-v0.1 at 72183037b00930a766f0f0f613d8689bdfd82e8e
**Predecessor tree:** 74ca6cc46b77bc1c13a3b1d2e736e0a56f246188

This plan separates declaration implementation, research, runtime reuse, book
generation, and publication. It authorizes none of them. The exact current
task is only the ten-path authority package listed in §2.

## 1. Preconditions for any later stage

Before any later task:

1. independently audit and accept the complete authority package;
2. verify the accepted branch, commit, tree, source identities, and predecessor
   reconstruction;
3. start from a clean, isolated worktree at the separately authorized base;
4. read every then-applicable authority and AGENTS.md;
5. identify exactly one authorized stage;
6. freeze its exclusive path manifest and validation tier prospectively; and
7. stop on any source, schema, hash, branch, stage, or scope mismatch.

No later stage is implied by completion of an earlier stage.

## 2. Current authority-drafting stage

The current candidate adds exactly:

1. CANONICAL_TOPOLOGY_MOTIF_PROGRAMME_FOUNDATION.md;
2. canonical_topology_motif_programme_contract.json;
3. canonical_topology_motif_programme_validation_contract.json;
4. canonical_topology_motif_programme_predecessor_manifest.json; and
5. CANONICAL_TOPOLOGY_MOTIF_PROGRAMME_IMPLEMENTATION_STAGING_PLAN.md.

It modifies exactly:

6. UNIFIED_PYTHON_RESEARCH_FRAMEWORK_SPECIFICATION.md;
7. UNIFIED_PYTHON_RESEARCH_FRAMEWORK_IMPLEMENTATION_PLAN.md;
8. POST_ATOMIC_OPEN_PROBLEM_REGISTER.md;
9. atomic_generator_book_traceability_manifest.json; and
10. EBU_FUTURE_BOOKS_STRUCTURE.md.

The current stage permits only documentation edits and static parsing, hashing,
schema, ordering, scope, Unicode, line-ending, Markdown-structure, Git-object,
and arithmetic checks. It forbids framework imports, project tests, fixture
materialization, scientific or synthetic model execution, transition calls,
benchmarking, rendering, manuscript/PDF generation, staging, commit, push,
tag, and publication.

## 3. Future Stage A — inert declarations and canonical identity

### 3.1 Authorization boundary

Stage A requires a new explicit implementation authorization after independent
acceptance of this package. It is not an I-6 substage and does not change
Bridge v0.2.

### 3.2 Exact prospective paths

Stage A may create only:

- src/ebu_framework/topology.py;
- tests/framework/fixtures/canonical_topology_v1.json; and
- tests/framework/test_canonical_topology.py.

It may make tightly scoped changes only to:

- src/ebu_framework/identity.py;
- src/ebu_framework/hashing.py;
- src/ebu_framework/errors.py; and
- src/ebu_framework/__init__.py.

A later Stage A authority must decide exact line-level responsibilities,
exports, failure ordinals, fixture bytes, and test counts before editing.
No other production or test path may be inferred.

### 3.3 Allowed implementation content

Stage A may implement:

- inert LayeredTopologyDeclaration;
- inert CanonicalTopologyWitness;
- inert RecursiveMotifDeclaration;
- inert CertifiedBoundarySummaryDeclaration;
- static-shape-only TopologyBenchmarkDeclaration;
- static-shape-only TopologyBenchmarkResult;
- pure formation and cross-field validators;
- CANONICAL_TOPOLOGY_SERIALIZE_V1;
- exhaustive color- and layer-preserving canonicalization for exactly
  \(0\le n\le8\);
- CANONICAL_TOPOLOGY_SIZE_UNSUPPORTED for \(n>8\);
- the dedicated CanonicalTopologyId digest preimage and typed result; and
- static conformance fixtures.

The benchmark declarations are inert envelope shapes. Their presence does not
permit a result to be computed or created.

### 3.4 Mandatory Stage A conformance cases

The future fixture and test must cover at least:

- relabeling equivalence;
- every permutation of unordered inputs;
- ordered-child reversal changes identity;
- cross-layer lookalikes differ;
- schema and digest-domain versions differ;
- unknown, malformed, dangling, duplicate, self-related, and cyclic cases;
- strict duplicate-key and non-finite JSON rejection;
- \(n=8\) exhaustive support and \(n=9\) typed refusal;
- identity independent of performance fields;
- Fibonacci bases and induction as static arithmetic;
- A1–A8 presence and each missing-assumption refusal;
- raw F13 correction upward-cone arithmetic for every \(Q,S\), including
  \(Q=S=\varnothing\);
- the explicit normalized convention
  \(I_{\mathrm{norm}}(\varnothing)=0\), distinct from
  \(I_{\mathrm{raw}}(\varnothing)=E(\varnothing)\);
- raw/normalized equality for every tested nonempty subset, including the
  alternating-subset-sum proof;
- an \(E(\varnothing)\) correction with both nonempty deltas equal to
  \((-1)^{|S|}\delta\), a nonempty contained correction, and a noncontained
  zero correction;
- exact nonzero-baseline pair and pure three-way examples;
- explicit consistency with unchanged Atomic F13;
- hidden cross-boundary reopening;
- arbitrary-table lower-bound witness;
- book no-bloat/excluded-concept scope;
- I-6 noninterference; and
- no forbidden call/import/result path.

### 3.5 Compatibility suite

The separately authorized Stage A validation must run its own tests plus:

- tests/framework/test_primitives_envelopes.py;
- tests/framework/test_i3_integration.py;
- tests/framework/test_i3a_declarations.py;
- tests/framework/test_i3b_declarations.py;
- tests/framework/test_i3c_declarations.py;
- tests/framework/test_i3d_declarations.py;
- tests/framework/test_atomic_declarations.py; and
- tests/framework/test_interaction_declarations.py.

The future authorization must first confirm which listed paths exist at its
accepted base. A missing or renamed path is not permission to substitute a
different test.

### 3.6 Stage A prohibited content

Stage A must not:

- infer topology from provider, action, event, network, result, or trace data;
- call any transition, step, simulation, trajectory, runner, experiment,
  optimization, or benchmark;
- calculate scientific EBU, interaction coefficients, or recursive surplus;
- add runtime cache lookup, storage, invalidation, or alias resolution;
- create research/topology/;
- create a benchmark protocol, result, figure, dataset, or atlas;
- change I-4, I-5, I-6, Bridge, a Gate, or publication mechanics;
- add a Fibonacci primitive to general framework behavior; or
- claim large-\(n\) canonical identity.

### 3.7 Stage A completion evidence

A future Stage A report must include:

- starting and final SHAs;
- exact changed paths and the accepted path-manifest hash;
- fixture raw and canonical byte identities;
- exact test names/counts and completed outcomes;
- confirmation that no model state or scientific experiment executed;
- exhaustive-bound evidence and above-bound refusal;
- I-6 noninterference;
- scope and diff checks;
- commit/push status; and
- next possible Stage B, explicitly not begun.

## 4. Future Stage B — production-independent topology research

Stage B requires separate analytical design, preregistration, implementation,
pre-execution validation, execution, interpretation, result, and publication
authorities. These remain distinct boundaries.

### 4.1 Reserved root and allowed research families

Only a separately authorized Stage B may create the production-independent
root research/topology/. It may then freeze exact paths for:

- full finite computations;
- certified compressed finite computations;
- strict enumeration;
- Fibonacci and Lucas controls;
- chain, tree, and balanced controls;
- random-recursive controls;
- perturbed-substitution controls;
- boundary-sufficiency negative controls;
- correction, alias, dependency, and invalidation experiments; and
- a versioned nonnormative atlas.

This authority names the root and research families only. It does not name
files, select parameters, freeze a protocol, create data, or authorize
execution.

### 4.2 Scientific protocol obligations

Before a Stage B execution, a frozen protocol and JSON plan must state:

- the hypothesis and claim class;
- exact motif families and controls;
- canonical identities and occurrence identities;
- initial augmented state, complete history, boundary, horizon, units, clocks,
  losses, burden, process, reserve, commitment, queue, delay, memory,
  settlement, and service semantics;
- feasible subset or poset domain;
- empty baseline without a zero assumption, removal semantics, finite subset
  policy, the all-subset raw F13 Möbius domain, and any separately named
  normalized nonempty convention;
- A1–A8 evidence or the exact negative condition under test;
- full versus compressed algorithms and comparison tolerance;
- correction and invalidation injections;
- randomness, seeds, tie-breaking, and ordering;
- outcome metrics including \(R_n\) and \(J_n\);
- falsifiers and negative controls;
- output-size and cost accounting;
- trace, durability, recovery, result, correction, and publication mechanics;
  and
- exact execution commands and invocation limits.

The full/compressed comparison must not be tuned after candidate outcomes are
inspected. Fibonacci is the first exact recursive worked family, not a favored
winner.

### 4.3 Stage B result separation

Structural identity, feasible-poset restriction, sparse support, additivity,
boundary compression, and correction locality must be reported separately.
A Stage B result must not convert:

- canonical equivalence into equal performance;
- support into active coefficient value;
- interaction into causality;
- positive \(J_n\) into fairness or universal benefit;
- a cache hit into scientific equivalence; or
- an atlas label into normative taxonomy.

I-6 evidence may be referenced only if its use is separately authorized and
its Bridge v0.2 meaning remains unchanged.

## 5. Future Stage C — no runtime reuse path

Stage C is refused at this coordinate. No committed cache module, database,
artifact name, key path, registry namespace, or runtime integration path is
reserved.

A future theorem/authority package would first need to freeze:

1. sufficient summaries for concrete query families;
2. exact query domains and feasible incidence structures;
3. complete dependency and alias graphs;
4. correction and invalidation semantics;
5. history-wide boundary equivalence;
6. interaction preservation under complete exposed-subset equality;
7. key completeness, performance, output-size, and collision obligations;
8. audit and provenance evidence;
9. correction governance; and
10. durability, recovery, finalization, and publication.

Until all ten are prospectively accepted, runtime reuse must fail closed.

## 6. Book and atlas stage

This plan authorizes no manuscript, PDF, renderer, figure, dataset, or atlas.
A later book task must use replacement/consolidation, preserve

\[
V\to\mu=\nabla V\to f_e\to\Psi_e\to J_e\to G_T\to
\text{finite EBU},
\]

and implement the exact Part I–III replacements in the foundation and
mechanical contract. The principal complete unit belongs in future Part VI
after sequential–parallel and higher-order interaction foundations. Future
Part VIII receives only a short cross-reference. The proof appendix and
separate nonnormative atlas require their own accepted source and evidence
identities.

No task may restore voltage, physical wave, phase-interference, Fermat-wave, or
superposition-as-EBU content.

## 7. I-6 preservation test

At every future stage, verify:

- Bridge remains exactly v0.2 unless separately amended;
- canonical topology is neither prerequisite nor blocker;
- no topology declaration enters the I-6 adapter contract;
- no I-6 path is added to a topology implementation manifest;
- no topology benchmark, Fibonacci primitive, inference, or cache is placed in
  I-6; and
- later Stage B use of I-6 evidence cites separate authority.

Any failure stops the topology task rather than expanding I-6.

## 8. Current completion boundary

The current package ends at independent authority-audit readiness. Its final
state must contain exactly five new and five modified authorized paths,
unstaged and uncommitted, with an empty index and no push or publication.

The next possible stage is independent audit. Stage A implementation, Stage B
research, Stage C runtime reuse, manuscript/PDF work, atlas creation, and
publication are not begun.

CANONICAL_TOPOLOGY_MOTIF_PROGRAMME_IMPLEMENTATION_STAGING_PLAN_COMPLETE
