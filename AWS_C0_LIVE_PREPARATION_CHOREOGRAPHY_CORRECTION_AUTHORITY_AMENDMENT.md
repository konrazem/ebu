# EBU AWS-C0 live-preparation choreography correction authority amendment

Status: **PROSPECTIVE ADDITIVE NON-SCIENTIFIC CORRECTION AUTHORITY CANDIDATE ONLY**

Authority ID:
`EBU-AWS-C0-LIVE-PREPARATION-CHOREOGRAPHY-CORRECTION-AUTHORITY-v1`

Accepted base: commit
`eec94950430c0f2811d0aea99b413cef35b72a7c`, tree
`8ea6df371e804866ad509bb251cfc25e0a447964`.

This candidate corrects only the prospective AWS-C0 preparation and live
authorization chronology. It adds no scientific authority, changes no Stage E
or Stage F fact, and authorizes no AWS call, mutation, spend, IAM change, image
operation, bootstrap, deployment, workflow execution, container start, or
scientific behavior while it is a candidate.

`AWS_C0_LIVE_PREPARATION_CHOREOGRAPHY_CORRECTION_AUTHORITY_CANDIDATE_COMPLETE`

## 1. Exact candidate scope

The candidate adds exactly six regular mode-`100644` files:

1. `AWS_C0_LIVE_PREPARATION_CHOREOGRAPHY_CORRECTION_AUTHORITY_AMENDMENT.md`;
2. `aws_c0_live_preparation_choreography_correction_contract.json`;
3. `aws_c0_live_preparation_choreography_correction_evidence_schema.json`;
4. `aws_c0_live_preparation_choreography_correction_implementation_path_manifest.json`;
5. `aws_c0_live_preparation_choreography_correction_predecessor_manifest.json`;
6. `aws_c0_live_preparation_choreography_correction_validation_contract.json`.

No accepted file may be modified. A seventh path, deletion, rename, mode
change, symlink, submodule, generated evidence file, ignored-state exclusion,
merge commit, nonlinear candidate history, force-push, or history rewrite
refuses this candidate.

Drafting and audit are static and offline except for writing these six files.
They may use read-only Git-object inspection, strict JSON and schema parsing,
canonical hashing, exact path and mode comparison, integer arithmetic, AST and
source-text inspection, and deterministic in-memory controls. They may not
contact AWS, Docker, a registry, a package index, or a project runner.

## 2. Exact predecessor and nonaccepted design source

The only accepted base is the commit and tree above. It is the normal
integration of the exact AWS-C0 authority and the exact one-path reachability
correction:

- authority candidate commit
  `f76f79b4ca68ea9466d31fba1e4627dc31f40679`, tree
  `d97add63617885305b0768371cce7a7828dbe35f`;
- authority integration commit
  `bf8fbe97f2c53cbcec91b27fa90d7ef6b0ad5118`, the same tree;
- reachability commit
  `2de79d7fc7451d9d32ecdf17e0a148b2246bdeb9`, tree
  `8ea6df371e804866ad509bb251cfc25e0a447964`; and
- reachability integration commit
  `eec94950430c0f2811d0aea99b413cef35b72a7c`, the same tree.

Every byte at that base is immutable in this candidate. The original authority
ID `EBU-AWS-C0-UNATTENDED-SYNTHETIC-REHEARSAL-AUTHORITY-v1`, its frozen
synthetic-only architecture, twelve-root evidence objective, failure closure,
cost bounds, single-use attempt, stopped-instance postcondition, and zero-
science boundary remain in force.

Later commits `88be7584bcb92e0f547beff202087fe85889f666` and
`80178bc27c16a1c7b7cecc473ab2895e3ab04a66`, both with tree
`d0595648a518413a37bb8dd97828b546da835ac4`, are classified exactly as
`NONACCEPTED_CIRCULAR_LIVE_PREPARATION_DESIGN_SOURCE`. They are descendants,
not predecessors, of the accepted base. They may supply reviewed source ideas
only. They are not accepted corrected implementation, validation evidence,
deployment authority, live packet, or execution evidence.

## 3. Defect and narrowly superseded chronology

The nonaccepted design required an AWS-assigned launch-object VersionId and a
live-authorization digest as CloudFormation inputs before change-set creation,
while also requiring the user authorization only after a complete packet that
included the returned change-set diff. AWS VersionIds and change-set identities
cannot be predicted, and an authorization cannot truthfully attest a packet
that does not yet exist. No digest convention may hide that cycle.

After this correction is independently accepted and integrated, only the
original authority's single temporal rule that placed every AWS mutation after
the final live packet is superseded for the exact preparation actions in
Section 7. The original requirement for a complete final live packet and a new
post-packet user authorization remains mandatory before change-set execution,
Step Functions execution, EC2 attempt start, systemd attempt start, or
synthetic-container start.

No general request to continue, prepare, deploy, test, or run is either of the
two exact approvals. Preparation approval is not live-attempt approval, and
live-attempt approval is not Stage F or scientific authority.

## 4. Acyclic records and byte identities

The corrected chronology uses these new closed records:

1. non-root `aws_c0_operator_bootstrap_packet/v1`;
2. non-root `aws_c0_operator_bootstrap_authorization/v1`;
3. non-root `aws_c0_operator_bootstrap_closure/v1`;
4. non-root `aws_c0_preparation_packet/v1`;
5. non-root `aws_c0_preparation_authorization/v1`;
6. root `aws_c0_launch_request/v2`;
7. non-root `aws_c0_preparation_closure/v1`;
8. non-root `aws_c0_live_packet/v1`;
9. non-root `aws_c0_live_authorization/v1`; and
10. root `aws_c0_start_receipt/v2`.

The twelve evidence-root categories and their order do not increase. Corrected
attempts use launch-request v2 and start-receipt v2 in the existing fourth and
fifth root positions. All other root categories retain their accepted version
unless a later exact correction separately authorizes a change. The eight new
operator-bootstrap, preparation, packet, and authorization records are
non-root control evidence;
they do not masquerade as additional roots.

Every JSON record is strict duplicate-free UTF-8 NFC, recursively key-sorted,
compact, integer-only, finite, and has no final line feed. Every root record's
`record_sha256` is the SHA-256 of its complete canonical record with only
`record_sha256` omitted. Its root key binds that preimage digest, while the
object receipt binds the SHA-256 of the complete stored bytes. No other root
field may be omitted or substituted.

A non-root control record contains no embedded self-digest. Its content-
addressed key and object receipt both bind the SHA-256 of the complete stored
bytes. An identity for any record has closed kind, and `value` and `sha256`
both equal the applicable root-preimage or complete-byte digest. AWS-assigned
VersionIds are opaque observed outputs and are never predicted, normalized, or
used as the digest preimage for the object that produces them.

The preparation and live authorization identities are hashes of complete
authorization records, not hashes of unauthenticated labels. Each authorization
record embeds the SHA-256 of the exact user statement bytes and a private
authenticated source identity. A SHA-256 proves byte integrity, not who issued
the statement; absent an authenticated source, authorization refuses.

Every field ending in `_identity`, and each semantically fixed bare `identity`
member, is validated against the contract's exact field-kind entry. Nullable or polymorphic fields use its machine-readable
`allowed_kinds`, `nullable_only_when`, IAM-action table, or action/resource
table; a generic identity schema never permits substitution. In particular,
workflow execution, SSM command, systemd invocation, and container identities
cannot be account identities.

## 5. Phase A: deterministic local build and read-only inventory

Only after corrected implementation integration and exact-target static CI,
the following packet-preparation inputs may be produced without an AWS
mutation or a preparation approval:

- exact Git, authority-audit, static-validation, and independent-audit
  identities;
- a reproducible finalizer ZIP and byte digest in a fresh private local
  directory;
- a synthetic-only image built from an already-present complete base digest
  with network disabled and pull forbidden, then exported and hashed, but
  never pushed or run;
- local hashes of the exact controller, service, workflow, SSM document,
  template, worker, image archive, and runtime policy;
- authenticated read-only AWS inventory through only the read actions in the
  contract; and
- a proposed exact IAM delta, artifact map, bootstrap payload, CloudFormation
  parameter derivation, deadlines, resource maxima, and cost ceiling.

If a base image, package, installer, credential, current fact, or required
identity is missing, the phase stops. Network acquisition, mutable package
installation, Docker pull, image push, Docker run, instance start, SSM command,
IAM mutation, object upload, and change-set creation are not read-only local
preparation.

Read-only inventory cannot prove stopped-host OS, architecture, Docker, AWS
CLI, installed controller, installed service, or loaded image state. Those are
bootstrap outputs and may not be invented from historical observations.
The retained-EBS rehearsal record supplies only historical expectations of
Docker `29.7.2` and AWS CLI `2.36.36`; the live bootstrap must verify both
exactly. It may not install, upgrade, or fetch either tool. A mismatch stops
preparation, closes it as FAIL or INCONCLUSIVE, and leaves the instance stopped.

### 5.1 Required operator-bootstrap prerequisite

An observed temporary AWS-root caller is not silently promoted into either
C0 approval. If Phase A does not observe an already usable exact constrained
operator role and preparation session, a separate identity prerequisite must
complete before the preparation packet exists. It is not one of the two C0
approvals and authorizes no C0 preparation or live action.

`aws_c0_operator_bootstrap_packet/v1` binds the exact account and temporary-
root caller identities, one exact proposed operator role and expected
`ABSENT` or `EXACT_PRESENT` before state, exact trust and inline base-policy
bytes, a closed permission-boundary disposition, the exact preparation-session
policy and a live-session policy ceiling identity, session names and maximum duration, required MFA and source
identity, a deadline, one retry, exact API/resource/call maxima, rollback, and
all zero-science fields. A mismatched existing role refuses; it is not edited
into shape under this prerequisite.

The only valid operator-bootstrap approval is UTF-8 NFC with no final line feed
and this exact ASCII field order:

```text
AUTHORIZE_AWS_C0_OPERATOR_BOOTSTRAP_V1 packet_sha256={packet_sha256} account_identity_sha256={account_identity_sha256} operator_role_identity_sha256={operator_role_identity_sha256} deadline_utc={deadline_utc} allow=CREATE_OR_VERIFY_ONE_CONSTRAINED_OPERATOR_ROLE,ASSUME_ONE_PREPARATION_SESSION deny=C0_PREPARATION_ACTIONS,C0_LIVE_ACTIONS,OTHER_ROLE,OTHER_SESSION,SCIENTIFIC_EXECUTION
```

The complete-byte `aws_c0_operator_bootstrap_authorization/v1` binds that
statement, packet, authenticated user source, and exact temporary-root caller.
Only then may that root caller use the packet's closed read set and, if the
role is absent, exactly `iam:CreateRole`, `iam:PutRolePolicy`, and
`iam:TagRole`; it may then call `sts:AssumeRole` exactly once for the named
preparation session with the exact preparation session policy, MFA and source
identity. Creation must use the packet's trust document, maximum duration,
tags, and optional exact existing permissions boundary. Rollback is limited to
packet-listed `iam:DeleteRolePolicy`, `iam:UntagRole`, and `iam:DeleteRole` for
that newly created role. There is no wildcard resource, managed-policy
creation, access-key creation, policy-version creation, trust-policy update,
other role, other session, or C0 service action.

`aws_c0_operator_bootstrap_closure/v1` closes every root call, exact role
readback, preparation-session identity/policy/expiry, rollback, and PASS, FAIL,
or INCONCLUSIVE disposition. PASS requires the exact role and live preparation
session; otherwise preparation stops. Immediately after the single assume-
role response, temporary root is barred from all C0 preparation and live
calls. The preparation packet binds this PASS closure and is created and
executed only by the observed constrained preparation session.

The three bootstrap records remain complete-byte-identified private local
control evidence at this gate. They contain no S3 VersionId or object receipt,
and root may not stage them. The later preparation packet binds the PASS
closure identity only. After the first C0 approval, the constrained preparation
session stages the exact three bootstrap records, preparation packet, and
preparation authorization at their fresh predetermined keys; the preparation
closure later binds all five returned object receipts.

The operator role's base policy is the exact bounded ceiling for the two later
session policies. The preparation session policy intersects that base policy
and explicitly denies change-set execution and workflow start. The final live
packet derives its exact live-session policy from the observed change-set
effect sets and proves it is a subset of both that ceiling and base policy.
After the final packet and second approval, that still-live preparation session
may self-assume exactly once into the packet-bound live session policy. Expiry of
the preparation session, role or policy drift, or inability to self-assume
requires a fresh operator-bootstrap packet and approval; root may not silently
reacquire a session.

## 6. Phase B: exact preparation packet and first C0 approval

Before any preparation mutation, present a human-readable packet and canonical
`aws_c0_preparation_packet/v1`. It binds:

- the exact PASS operator-bootstrap closure and exact authority,
  implementation, account, Region `us-east-1`, instance
  `i-048bac00bdb540a4e`, constrained assumed-role operator session and session-
  policy identities, session expiry, and initial `stopped` identities; AWS root,
  an IAM user with long-lived keys, or an unbounded principal refuses;
- exact local artifact byte counts and SHA-256 values;
- one exact existing S3 bucket whose Region, versioning, encryption, public-
  access-block, and applicable KMS identities are read and bound; a fresh
  preparation prefix and exact predetermined keys; output-derived record-key
  rules; and maximum puts, gets, heads, lists, and bytes;
- the exact instance-profile and sole-role identities, canonical IAM before
  state, every permitted remediation and inverse rollback action, exact policy
  bytes, and canonical expected after state;
- one bounded bootstrap start/SSM/stop plan, document/version/payload digest,
  allowed installation paths, expected host/tool/image facts, maximum calls,
  deadline, EC2 seconds, and mandatory final `stopped` state;
- exact template hash, stack name, change-set name and type, parameter-
  derivation identity, capabilities, role disposition, and create-only rule;
- exact preparation cleanup, retry count, deadline, currency, cost model,
  ceiling, and maximum resource use; and
- every zero-science counter at zero and every forbidden action.

The only valid first approval statement has the exact ASCII field order below,
is encoded as UTF-8 NFC, and has no final line feed:

```text
AUTHORIZE_AWS_C0_PREPARATION_V1 packet_sha256={packet_sha256} account_identity_sha256={account_identity_sha256} region=us-east-1 instance_id=i-048bac00bdb540a4e deadline_utc={deadline_utc} cost_ceiling_minor_units={cost_ceiling_minor_units} allow=EXACT_IAM_REMEDIATION,IMMUTABLE_S3_STAGE,BOUNDED_HOST_BOOTSTRAP_START_SSM_STOP,CREATE_CHANGE_SET_ONLY deny=EXECUTE_CHANGE_SET,START_EXECUTION,CONTAINER_RUN,ECR_PUSH,SCIENTIFIC_EXECUTION,REPLAY
```

Brace-delimited tokens above denote fields deterministically rendered from the
complete packet; they are not values permitted in a sealed statement. The
authorization record binds the exact rendered statement digest, preparation-
packet complete-byte identity, authenticated source identity, and observation
time. A changed byte, expired deadline, wrong source, or mismatched packet
refuses.

## 7. Phase C: preparation-only AWS actions

The first approval permits only the following ordered, bounded actions against
the exact packet resources:

1. repeat the read-only account/Region/state/IAM/S3/quota preflight;
2. apply only enumerated IAM remediation with `iam:PutRolePolicy`,
   `iam:DeleteRolePolicy`, `iam:AttachRolePolicy`, or
   `iam:DetachRolePolicy`, then read back the exact expected policy-set digest;
3. use only `s3:PutObject` for the listed fresh keys, including the three
   operator-bootstrap records and two preparation-control records, with
   `If-None-Match: *`,
   `ChecksumSHA256`, bucket versioning `Enabled`, and the sealed encryption
   disposition; retain and exact-version-read every returned VersionId;
4. call `ec2:StartInstances` only for the exact stopped instance, deliver only
   the exact versioned bootstrap with `ssm:SendCommand`, poll it within the
   packet bounds, install only checked artifacts at allowed root-owned paths,
   verify rather than install Docker `29.7.2` and AWS CLI `2.36.36`, load but
   never run the synthetic image, and call `ec2:StopInstances` on
   success or failure until the bounded final observation is `stopped`;
5. create and stage the fresh private snapshot, launch-request v2, and their
   receipts using only the closed output-derivation rules;
6. call `cloudformation:CreateChangeSet` exactly once for the named stack and
   change set, with execution forbidden; poll only through the review deadline
   and retain the canonical returned diff; and
7. stage exactly one preparation closure and one final live packet derived
   from those outputs, then stop with the change set unexecuted.

KMS `Encrypt`, `Decrypt`, `GenerateDataKey`, and `DescribeKey` may occur only
for the exact sealed KMS key when the bucket requires them. All preparation
read APIs, mutation APIs, resource selectors, call maxima, and support actions
are closed in the contract. An omitted resource, wildcard selector, additional
API, direct `CreateStack`, `ExecuteChangeSet`, `StartExecution`, document
creation, S3 delete, object overwrite, ECR action, container run, or project
command refuses.

Opaque outputs do not make the approval open-ended. Every approved derived
object is the unique canonical result of packet inputs plus authenticated AWS
responses under a closed schema. Exactly one instance may start, exactly one
bootstrap command may be sent, and exactly one change set may be created. The
change set may create a `REVIEW_IN_PROGRESS` shell, which is retained rather
than silently deleted. Its deletion or recreation requires later exact
authority.

The preparation caller must be the packet's exact assumed-role session and
must carry the packet's closed session policy with explicit deny for
`cloudformation:ExecuteChangeSet`, `states:StartExecution`, unlisted S3 and SSM
resources, container or scientific services, and all unlisted mutations. The
historical temporary AWS-root login is not an acceptable preparation or live
operator. Caller identity and session expiry are rechecked before each
mutation; session replacement requires a new packet and approval.

Preparation failure permits only bounded stop cleanup and the exact inverse
IAM actions already present in the packet. No S3 object is deleted or
overwritten. Failed rollback, failed stop, unexpected after state, partial
staging, expired change set, or drift closes as typed preparation FAIL or
INCONCLUSIVE and cannot proceed to a live packet.

## 8. Phase D: preparation closure and complete final live packet

`aws_c0_preparation_closure/v1` binds the preparation packet and first
authorization, every attempted action and disposition, IAM before/after/
rollback identities, every exact S3 object receipt, bootstrap facts, stopped
postcondition, private snapshot, launch-request-v2 receipt, change-set
observation and canonical diff, costs, resource use, and a closed PASS, FAIL,
or INCONCLUSIVE disposition. Only PASS is live-packet eligible.

PASS is recomputed, never trusted from its label: all ten ordered preparation
phases must be successful; bootstrap must be complete and stopped with exact
tool/image facts; every object receipt must exist; the change set must be
available and unexecuted; and integer cost-model recomputation must prove the
upper bound does not exceed the sealed ceiling. FAIL and INCONCLUSIVE require
nonnull typed failure fields and never advance. UTC values are parsed and
strictly ordered; an execution-session expiry equal to or before its live-
authorization observation refuses.

The complete `aws_c0_live_packet/v1` binds the PASS closure, exact
launch-request-v2 root and version receipt, the unexecuted reviewable change
set, all original AWS-C0 live envelope fields, final read-only preflight,
stopped state, attempt and cleanup limits, cost envelope, and zero-science
counters. It is staged once at a fresh exact key; its returned VersionId,
complete-byte SHA-256, and byte count are part of the human packet publication
observation. The live packet does not contain an authorization that has not yet
been issued.

The live packet binds the exact still-live constrained preparation session and
expiry, its operator role, and one intended live-session policy identity and
maximum duration, but not an assumed live session
that cannot exist before the second approval. That policy permits only
execution of the exact change-set identity, the template- and diff-derived
closed API/resource effect sets required to realize it with no CloudFormation
service role, readback of the resulting exact stack, and one named
`states:StartExecution` call for the exact state machine and attempt; it
explicitly denies every other change set, execution, attempt, AWS mutation,
and scientific service. Root and long-lived IAM-user credentials refuse.
The effect set includes `iam:PassRole` when, and only when, the exact template
assigns the exact Lambda or Step Functions execution roles; every other
`PassRole` resource refuses. The preparation phase itself never receives
`iam:PassRole`.

Immediately before presenting the packet, re-read the change set, instance,
profile, sole role, IAM policy-set, bucket Region/versioning/encryption/public-
access-block and applicable KMS controls, the instance's VPC/subnet/interface/
route/public-address/NAT/endpoint path, quota, and artifact versions. ECR has
the closed disposition `NOT_USED_PRELOADED_IMAGE_NO_ECR_API`; no ECR account
inventory is claimed or required because preparation stages an image archive
in S3 and the attempt uses only that preloaded digest. Drift, expiration,
replacement, a changed diff, a wrong initial state, or a stale observation
refuses and requires a fresh preparation identity and approval; no field is
patched in place.

## 9. Phase E: second approval and one live attempt

The only valid second approval statement has this exact ASCII field order,
UTF-8 NFC encoding, and no final line feed:

```text
AUTHORIZE_AWS_C0_LIVE_V1 live_packet_sha256={live_packet_sha256} live_packet_key={live_packet_key} live_packet_version_id={live_packet_version_id} change_set_identity_sha256={change_set_identity_sha256} account_identity_sha256={account_identity_sha256} region=us-east-1 instance_id=i-048bac00bdb540a4e attempt_identity_sha256={attempt_identity_sha256} allow=ASSUME_EXACT_LIVE_SESSION,EXECUTE_EXACT_CHANGE_SET,START_ONE_EXACT_EXECUTION deny=REPLAY,OTHER_CHANGE_SET,OTHER_ATTEMPT,SCIENTIFIC_EXECUTION
```

The rendered statement binds the already complete live-packet object, exact
unexecuted change set, account, Region, instance, and attempt. The later
`aws_c0_live_authorization/v1` embeds its exact statement SHA-256, authenticated
source identity, observation time, and those packet coordinates. Its complete-
byte SHA-256 is the live-authorization identity. This construction is acyclic:
the launch request and change set exist before the statement, and the live
authorization is supplied only as closed execution input afterward.

The second approval permits the still-live constrained preparation session to
self-assume exactly once into the packet-bound live session and then permits
only execution of that change-set identity and one named Step Functions
execution for the one attempt. The live-authorization record binds the
observed live-session identity and expiry. The workflow input is
exactly the closed live-authorization envelope; arbitrary or additional input
refuses. Before any `StartInstances`, finalizer preflight independently fetches
the exact live-packet VersionId, verifies complete bytes and all transitive
identities, reconstructs the statement, verifies its SHA-256 and authenticated
source, verifies the change set was the approved executed change set, rechecks
the private infrastructure bindings and stopped state, and emits or retains a
typed refusal on failure.

The SSM source sidecar and start-receipt v2 bind the live-packet and live-
authorization identities. The controller independently exact-version-fetches
and verifies the launch and packet rather than trusting environment or SSM
labels. Later attempt evidence remains tied to the single consumed attempt and
unique start root. No automatic replay is permitted.

## 10. Zero-science and Stage F boundary

Every correction record carries exactly these ten zero counters:

- `project_runner_import_count=0`;
- `ebu_framework_import_count=0`;
- `stage_e_harness_import_count=0`;
- `registered_configuration_count=0`;
- `model_state_advance_count=0`;
- `trajectory_count=0`;
- `simulation_or_gate_count=0`;
- `scientific_rng_draw_count=0`;
- `outcome_inspection_count=0`; and
- `scientific_output_count=0`.

Preparation may inspect infrastructure and build inert synthetic artifacts. It
may not import a project runner, EBU framework, Stage E harness, configuration,
model, scientific RNG, trajectory, simulation, Gate, scientific output, result,
figure, interpretation, book, release, or publication path. No scientific
payload may be staged, installed, invoked, inferred, or claimed.

`stage_f_execution_authorized=false` remains exact. Neither approval authorizes
Stage F, a scientific image, scientific configuration, scientific execution,
scientific interpretation, or publication. AWS-C0 PASS remains non-scientific
operability evidence for one exact attempt only.

## 11. Exact later repository scope

After independent PASS and normal integration of this six-file candidate, one
separately audited reachability correction may modify exactly
`tests/framework/test_validation_reachability.py`. It preserves all accepted
phases and adds only the literal correction-authority and corrected-
implementation phases frozen in the implementation path manifest.

Only after that reachability correction is independently accepted and
integrated may the corrected implementation add the same exact fourteen paths
already authorized at the accepted base. It adds no fifteenth path and modifies
no accepted path. The correction changes prospective content requirements,
not the fourteen-path set. The nonaccepted circular design commits are not an
implementation base and must not be merged or cherry-picked as a substitute.

Dedicated CI remains credential-free, offline, static, zero-science, and exact-
target. It may not use AWS credentials, OIDC, Docker, a network, project code,
or live preparation. The original 66 authority cases remain their own frozen
arithmetic; the correction contract's 84 cases neither alter nor substitute
for them.

## 12. Required independent gates

The mandatory order is:

1. fixed-coordinate independent audit of these six correction files;
2. normal integration of the exact audited correction candidate;
3. exact one-path correction reachability change and independent audit;
4. normal correction reachability integration;
5. exact corrected fourteen-path implementation and static validation;
6. independent implementation, choreography, IAM, and zero-science audit;
7. normal implementation integration and dedicated exact-target static CI;
8. deterministic local build and authenticated read-only inventory;
9. verify a still-live operator-bootstrap PASS closure, or complete a fresh
   operator-bootstrap packet when none exists;
10. for a fresh packet only, exact operator-bootstrap user approval issued
    after that packet;
11. for a fresh packet only, root-limited role creation or verification, one
    preparation-session assumption, and closed PASS operator-bootstrap evidence;
12. complete preparation packet bound to that PASS evidence;
13. exact first C0 user approval issued after that packet;
14. preparation-only execution and closed PASS evidence;
15. outcome-blind private infrastructure/IAM/change-set review and complete
    final live packet;
16. exact second C0 user approval issued after that packet; and
17. only then, exact live-session assumption, change-set execution, and one
    synthetic AWS-C0 attempt.

Self-review is not independent. A failed, skipped, partial, filtered, stale,
zero-check, unsealed, or missing gate is not PASS. No phase advances
automatically after completing its predecessor.

## 13. Mandatory refusals

Refuse on a wrong base/tree; any altered accepted byte; a seventh authority
path; AWS root outside the exact operator-bootstrap packet and approval,
long-lived IAM-user credentials, a non-assumed-role preparation or live
operator, an unbound or expired session policy, arbitrary API strings, or a caller
change; a merge or nonlinear candidate; acceptance or integration of either
nonaccepted circular-design commit; a wildcard or dynamic path scope; a
fifteenth implementation path; an old v1 launch for a corrected attempt; live
authorization inside a pre-approval launch or CloudFormation parameter; a
packet that predicts an AWS VersionId or change-set identity; operator-role
bootstrap without its exact prerequisite packet and approval; root use for a
C0 preparation or live action; another role or session; a mismatched existing
operator role; a pre-preparation-approval bootstrap or preparation record that
claims an S3 VersionId or object receipt; an approval not
issued after its exact packet; a statement byte/source/digest mismatch; any
unlisted API/resource/action; preparation without the first approval;
change-set execution or workflow start under preparation approval; live action
without the second approval; a null CloudFormation service role without exact
change-set effect API/resource sets in the subset-proven live-session policy;
arbitrary workflow input; any path to instance
start that bypasses authorization preflight; drift; overwrite; reuse; missing
VersionId/checksum/byte receipt; mutable package, image, SSM, controller,
service, workflow, finalizer, policy, or template identity; a bootstrap that
installs or fetches Docker or AWS CLI, accepts a tool-version mismatch, or does
not end stopped; incomplete IAM rollback evidence; unbounded time,
resource, retry, or cost; a credential in Git; unauthenticated user-source
identity; any nonzero science counter, scientific field/import/action/output,
or Stage F claim; or any missing typed closure.

Independent acceptance of this candidate means only that its prospective
correction is ready for normal integration. It authorizes no reachability or
implementation change, AWS or Docker action, spend, preparation packet,
approval inference, deployment, rehearsal, scientific work, interpretation,
release, or publication.
