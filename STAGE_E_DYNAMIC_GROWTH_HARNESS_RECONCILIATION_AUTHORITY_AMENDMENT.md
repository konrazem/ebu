# Stage E dynamic-growth harness reconciliation authority amendment

Status: **PROSPECTIVE ADDITIVE AUTHORITY ONLY**

Authority ID: `EBU-STAGE-E-DYNAMIC-GROWTH-HARNESS-RECONCILIATION-v1`

Accepted private base: commit `08cea14d828668413b9156da8f220beec2713c26`, tree `a1b690662e14eab7220492dd378fce93b15eb9c7`.

This amendment reconciles the independently accepted Stage E scientific-harness authority with the later, independently accepted Stage D dynamic-growth campaign authority. It does not implement the harness and does not execute a registered study. The six accepted Stage E authority files, the six accepted Stage D scientific-validation files, the five completion-oriented continuation files, the five dynamic-growth files, the workflow, and the current reachability test remain byte-for-byte inputs.

`STAGE_E_DYNAMIC_GROWTH_HARNESS_RECONCILIATION_AUTHORITY_COMPLETE`

## 1. Exact authority scope

The authority candidate adds exactly these six regular mode-`100644` files:

1. `STAGE_E_DYNAMIC_GROWTH_HARNESS_RECONCILIATION_AUTHORITY_AMENDMENT.md`;
2. `stage_e_dynamic_growth_harness_reconciliation_contract.json`;
3. `stage_e_dynamic_growth_harness_reconciliation_evidence_schema.json`;
4. `stage_e_dynamic_growth_harness_reconciliation_implementation_path_manifest.json`;
5. `stage_e_dynamic_growth_harness_reconciliation_predecessor_manifest.json`;
6. `stage_e_dynamic_growth_harness_reconciliation_validation_contract.json`.

No accepted file is modified by this authority candidate. A seventh path, a deletion, a rename, a mode change, a symlink, an untracked output, a scope-derived exclusion, a force-push, or a history rewrite refuses the authority candidate.

## 2. Authority relationship and precedence

This amendment is additive. It does not supersede or weaken any accepted Stage D, continuation, Stage E, Stage C, packaging, framework, conservation, topology, interaction, or CLCD authority.

The accepted Stage D dynamic-growth authority explicitly records `stage_e_authority_reconciliation_required=true` and `stage_e_implementation_authorized=false`. Therefore the earlier Stage E authority cannot itself authorize implementation of the dynamic-growth programme. This amendment supplies only that missing prospective Stage E harness authority and closes independently observed conformance gaps.

If this amendment and any accepted predecessor cannot be satisfied simultaneously, implementation refuses and the conflict returns to an independently audited prospective authority correction. No implementation convenience may resolve an authority conflict.

## 3. Rejected diagnostic harness coordinate

The existing private harness candidate `f2e8753db5be1ca8809bbbd8e2e59a0630288d46`, tree `24404819230883a139dd41fefa2574548560eae2`, is diagnostic input only. It has no implementation PASS and is not an accepted base. Its exact 46-path scope is useful prospective material, but its bytes, green self-checks, and CI are not scientific evidence and are not accepted implementation evidence.

The replacement Stage E harness must close all five independently reported defects:

1. it must validate every frozen positive schema fixture directly, not infer a count by arithmetic or validate only a representative subset;
2. it must construct and apply every frozen negative mutation to its named complete base fixture, not validate `{}` or another generic invalid object as a substitute;
3. DAG evidence must retain exact indegree-initialization, enqueue, dequeue, edge-inspection, queue-append, head-advance, and ready-comparison counters, plus separately accounted canonicalization comparisons and auxiliary edge storage;
4. every frozen complexity cell must retain exact logical output-byte count;
5. dependency-local cache invalidation controls must traverse declared nonempty dependency and alias edges and prove the complete affected closure; an empty-edge call cannot establish the control.

Any replacement that preserves the old evidence label while omitting one of these corrections refuses.

## 4. Prospective implementation closure

The accepted Stage E manifest authorizes one workflow modification and 45 new paths. This amendment retains those 46 paths and adds exactly five new paths:

- `stage_e_harness/growth.py`;
- `stage_e_harness/recursive.py`;
- `stage_e_harness/capacity_population.py`;
- `tests/stage_e/test_dynamic_growth_conformance.py`;
- `tests/stage_e/test_capacity_population_conformance.py`.

The prospective completed harness therefore changes exactly 51 paths relative to the accepted base: one existing workflow path and 50 new regular files. The workflow modification is still append-only and must preserve the accepted 13,830-byte Stage C prefix. The appended Stage E job may be prospectively extended only to include the exact reconciliation authority, the exact five new source/test paths, and their sealed evidence. Existing T0, T1, T2, conventional, packaging, release, and fail-closed routing remain unchanged.

The implementation-path manifest is preserved byte-for-byte because its path
closure is unchanged. Its `scripts/validate_stage_e_harness.py` rule contains
the rejected provisional literals “63 fixtures” and “247 exact refusal cases.”
Those two count literals are explicitly non-operative and are prospectively
superseded, only for that one rule, by the mechanically executable 61-fixture
and 248-refusal registries in this amendment, contract, schema and validation
contract. The path, no-substitution, no-count-only and complete-execution
requirements in that rule remain binding. Treating 63/247 as executable
counts, or changing any other manifest byte or meaning, refuses.

The three new modules have closed roles:

- `growth.py` implements outcome-blind structural-growth fixture construction, full/incremental/reuse reconstruction comparison, exact work counters, topology receipts, and invalidation receipts;
- `recursive.py` implements the direct recursive two-child Möbius oracle, inverse and forced-recurrence checks, feasible-poset oracle, coefficient-transport/scaling oracle, correction propagation, and non-scalable refusal;
- `capacity_population.py` implements only the exact reduced-rational capacity, burden, allocation, demographic bookkeeping, service, viability, homeostasis, recovery, collapse, stall, comparison, counter, and cap relations frozen by Stage D.

They must not import an EBU model, invoke a project runner, advance state, construct a registered scientific configuration, inspect a candidate outcome, or produce a scientific result.

## 5. Complete schema replay, not arithmetic bookkeeping

The Stage E schema lane must derive the exact supported Draft 2020-12 vocabulary from all four controlling evidence schemas:

1. `stage_d_scientific_validation_evidence_schema.json`;
2. `stage_d_completion_oriented_continuation_evidence_schema.json`;
3. `stage_e_scientific_harness_evidence_schema.json`;
4. `stage_d_dynamic_growth_campaign_evidence_schema.json`.

Unknown assertion, applicator, or authority-metadata keywords refuse validator construction. Metadata is never silently treated as an instance assertion.

The derived standard vocabulary is exactly 29 keywords. It includes
`$comment`, as used by the accepted dynamic-growth schema. `$comment` is an
accepted annotation keyword: it is consumed, never used to decide instance
validity, and receives an exact annotation/non-reliance conformance case.

The positive fixture ledger contains exactly 61 complete immutable non-evidence fixtures:

- four accepted Stage D object fixtures; the adjacent `purpose` string is
  authority metadata and is never validated as an instance;
- two accepted continuation object fixtures; its adjacent `purpose` string is
  likewise metadata;
- eleven reconciliation-owned complete Stage E v1 record fixtures which
  materialize the eleven accepted Stage E fixture labels;
- forty-four accepted dynamic-growth fixtures.

The exact registry freezes, for every row, its source document and JSON
Pointer, target root or definition, fixture ID, canonical byte identity, and
ordinal. Every fixture is loaded or materialized from those committed bytes,
hashed, validated against its named root or definition, and recorded with one
ledger identity. A count derived without those 61 ledger rows is not a pass.

The refusal ledger contains exactly 248 applied cases:

- sixteen accepted Stage D negatives;
- thirty-five accepted continuation negatives;
- twenty-four accepted Stage E negatives;
- one hundred fifty accepted dynamic-growth exact patches;
- twenty-two accepted assertion/applicator keyword mutations;
- one `$comment` annotation/non-reliance mutation.

For each case the validator must use the committed 248-row refusal registry.
Each row freezes case ID, source case pointer, named complete base fixture,
target definition, exact RFC-6902 patch or complete mutated instance, patch
identity, mutated-instance identity, structural-validity expectation,
validation layer, semantic-rule ID where applicable, and disposition. The
validator loads the named base, applies the exact patch, recomputes both
identities, executes the named structural or semantic layer, and records the
exact refusal. A generic empty object, a different mutation, a count-only
claim, a skip, or an expected failure without executing the mutation is
`NOT_A_PASS`.

An inherited negative whose purpose is to refuse a floating JSON token is
stored as exact UTF-8 raw JSON text plus its raw byte identity, never as a
floating number inside an authority document. Its validation layer is
`JSON_PARSE`; parsing it with the integer-only authority parser must refuse.

The 150 dynamic-growth patches retain their exact split: 116 refuse structurally and 34 remain structurally valid but refuse under their named semantic relations. Structural rejection of one of the 34 semantic cases is not interchangeable evidence.

## 6. Recursive Möbius and topology oracles

Stage E must implement a direct, independently understandable oracle before any optimized or reused path.

For every frozen two-child macro table, the direct Boolean coefficient is

`I_n({x,y}) = e_n({x,y}) - e_n({x}) - e_n({y}) + e_n(empty)`.

It must equal the accepted `J_n` exactly, including nonzero `E(empty)`, positive and negative `J_n`, and the declared common protocol, boundary, units, history, feasibility, removal, and settlement semantics. The inverse reconstruction

`R_(n+1) = R_n + R_(n-1) + J_n`

and forced projection

`R_n = F_n R_1 + F_(n-1) R_0 + sum_(k=1)^(n-1) F_(n-k) J_k`

are checked with exact integer or reduced-rational arithmetic. The omitted-interaction error and correction multiplier are algebraic dependency propagation only. They are never described as a physical wave or universal causal law.

The exact oracle domain is fixed at 270 direct macro tables from 18 sequence/baseline instances and levels 1 through 15. It includes homogeneous Fibonacci, Lucas-compatible, Lucas-incompatible, positive impulse, negative impulse, and alternating fixtures. Perturbed recursion, broken A1-A8, stale-cache, and nonrecursive topology are refusal or negative controls. Fibonacci and Lucas remain conditional examples, not population laws, optimality claims, or universal EBU laws.

For each declared feasible poset, the harness checks

`I(x)=sum_(y<=x) mu_P(y,x) E(y)`,

`E(x)=sum_(y<=x) I(y)`, and

`delta I(x)=sum_(q<=x) mu_P(q,x) delta E(q)`

on exactly 32 base transforms and 224 one-element correction cases. It never invents Boolean values for infeasible subsets.

## 7. Coefficient transport, scaling, and residuals

The harness must compare every scaled prediction with a direct target coefficient. A role-preserving map, typed provider/domain/model authority or accepted derivation, source and target units, query equivalence, boundary equivalence, factor, residual, dependency projection, alias projection, correction epoch, numerical policy, code, artifact, and environment identity are mandatory.

The exact Stage E domain is:

- 90 valid scalar transport rows;
- 15 non-scalable direct rows;
- 180 correction reconstructions;
- 150 misscaling preflight refusals;
- zero misscaled trajectories.

For a valid scalar row,

`K_(n+1)(iota_n S)=I_(n+1)(iota_n S)-lambda_(n,S) I_n(S)`

is reconstructed exactly and remains visible. For a correction,

`delta I_(n+1)=lambda delta I_n + delta K`.

Invariant/intensive, occurrence-extensive, boundary-extensive, degree-homogeneous, scale-dependent-residual, and non-scalable/direct classes remain distinct. A missing map, factor, units, query equivalence, authority, direct target, or residual refuses scaled reuse. No factor is inferred automatically. The Fibonacci or Lucas occurrence ratio is admitted only for its proved occurrence-extensive family.

## 8. Growing-topology reconstruction conformance

Stage E does not execute the 396 registered scientific runs or the 8,192-tick campaign. It uses accepted non-scientific fixtures and bounded microcases to prove harness mechanics before Stage F.

For every admitted conformance triplet, full reconstruction, incremental reconstruction without reuse, and certified motif reuse must emit byte-identical canonical scientific-output projections before any efficiency statement. The conformance output is labelled `NON_SCIENTIFIC_SYNTHETIC_FIXTURE` and has no scientific sign.

The harness records exact topology version, old-subset and coefficient projections, complete cache key, A1-A8 and boundary certificates, dependency and alias edges, affected closure, invalidation radius, propagation depth, recomputed and reused coefficient counts, cache hits/misses/stale hits, storage, trace bytes, output bytes, and receipts.

An unchanged old-subset domain may retain a coefficient only under the accepted incremental-extension theorem. A changed old subset, external interaction, boundary, history, feasibility, removal, settlement, authority, numerical policy, map, factor, residual, dependency, or alias input deterministically invalidates the complete affected upward closure before recertification. A broad-reconfiguration fixture must produce a broad closure. A local fixture must still traverse at least one nonempty dependency edge and at least one nonempty alias edge where its declared topology contains them.

The wrong A-equals-B equivalence assignment, random/nonrecursive topology, stale cache, incomplete key, broken certificate, missing invalidation, and boundary-crossing local-reuse claim refuse. No topology compression result is a viability or homeostasis result.

## 9. Capacity-population conformance

Stage E implements the exact Stage D capacity-population records and semantic relations only as non-scientific fixtures.

The recursive and direct nonrecursive capacity branches are mutually exclusive. `CP-NONRECURSIVE` preserves `C_0=2` and `C_1=3`, applies its direct hash rule only to targets 2 through 16, has null residuals, and never invokes the recursive equation.

For recursive cases the harness checks the exact burden-indexed equation

`C_(n+1)=C_n+C_(n-1)+J^C_n-X_(n+1)`

and the complete six-component receipted burden. Positive raw surplus is never relabelled as usable capacity without the declared conversion and costs.

For an eligible separately declared demographic response `P_n=rho C_n`, it checks only the conditional projection

`P_(n+1)=P_n+P_(n-1)+rho(J^C_n-X_(n+1))`.

Demand-to-topology, topology-capacity-to-demography, and bidirectional feedback arms remain distinct. Canonical births, deaths, migration, reserve allocation, quality allocation, response delay, shock, demographic transition, nonrecursive, zero/negative residual, and positive-but-nonconvertible controls are mandatory.

Resource/service equations, viability, homeostasis windows, recovery, collapse, stall, terminal homeostasis, comparison, cap, and continuation relations must be reconstructed from the 44 complete fixtures and 34 accepted semantic mutations. A missing Arm-3 request produces the closed non-scientific stall record and prohibits later rows; it never fabricates a target row.

Stage E does not populate the 900 scientific level rows, 720 windows, 450 requests, or 216 recovery events. It proves that records representing those future counts can be checked and that the accepted fixtures produce their frozen local relations.

## 10. DAG and cache evidence corrections

The optimized DAG path remains canonical dense-index FIFO Kahn traversal after separately accounted canonical edge sorting. Traversal evidence records all of:

- exactly `V` indegree initializations;
- at most `V` enqueues;
- at most `V` dequeues;
- exactly `E` edge inspections;
- at most `V` queue appends;
- at most `V` head advances;
- zero ready-node comparisons.

Canonicalization records exact comparison count and auxiliary edge slots separately. The traversal claim remains `O(V+E)`; canonicalization is bounded separately by `E*ceil(log2(E))` comparisons and `E` auxiliary edge slots. Every registered edge is inspected exactly once. Canonicalization input-edge count equals traversal edge count. The five cells are exactly and uniquely `(V,E)=(128,256)`, `(1024,4096)`, `(10000,50000)`, `(100000,500000)`, and `(512,130816)`. Every cell retains its exact identity, input vertices, input edges, all exact counters, canonicalization counters, wall time, peak process-tree RSS, storage bytes, trace bytes, and logical output bytes.

The cache-control evidence must include a closed declared key universe and
explicit nonempty typed `{source_key_identity,target_key_identity}` dependency
and alias edge arrays for controls that claim dependency-local invalidation.
It records the changed seed, expected and observed affected identity arrays,
ordering, correction epoch, pre/post cache epochs, exact invalidated,
recomputed and reused identity arrays, and durable receipt identity. Every
edge endpoint must belong to the key universe; expected and observed closures
must equal direct traversal; invalidated and recomputed arrays equal the
affected closure; reused identities are disjoint. Counts may be derived but
cannot replace identities. Empty arrays cannot establish traversal.
Near-equivalent negative controls, incomplete-key collisions, stale-cache
use, authority changes, scaling changes, and correction changes refuse.

## 11. Complexity evidence and nonclaims

Stage E complexity work is outcome-blind harness verification, not a registered scientific campaign.

Every complexity cell runs in a fresh process with no discarded warmup or repetition. It retains exact algorithm-specific operation counters, monotonic wall nanoseconds, reconciled process-tree peak RSS, storage bytes, trace bytes, and logical output bytes for every repetition. Missing output bytes or a count inferred from another field refuses the cell.

The Boolean Möbius boundary remains exact `O(n*2^n)` arithmetic plus subset-evaluation cost and `O(2^n)` storage. It does not promise arbitrary-N completion. The recursive-summary corollary is conditional on A1-A8 and bounded composition; it never applies to physical construction, storage, data collection, coefficient acquisition, boundary certification, or arbitrary primitive all-subset reconstruction.

Stage E may measure only the frozen oracle, schema, DAG, cache, transport, and bounded dynamic-growth conformance cells. It must not run the 396 registered trajectories, the 79,567,392-evaluation scientific estimate, or any candidate scientific configuration. Stage F duration and resource estimates are projections from Stage E throughput and registered counts, clearly labelled as projections.

## 12. Evidence and artifact closure

The completed Stage E harness must emit a deterministic standard-library-only zip application in three perturbed replicas. Every member is regular, safe, ordered by UTF-8 path bytes, stored with the frozen timestamp and mode, and byte-identical across the three replicas. This is not independent-build evidence.

The zip application embeds byte-exact accepted Stage D, continuation, Stage E, dynamic-growth, and reconciliation authority inputs with a canonical identity index. It contains no registered configuration, scientific output, result, figure, book, publication material, network fallback, mutable dependency, or hidden source-checkout import.

Installed direct-wheel and sdist-derived-wheel lanes run outside the source checkout with isolated environment variables and verify that the harness does not import the checkout accidentally. All existing Stage C T0/T1/T2, conventional, packaging, installed-surface, runtime, archive, license, and reproducibility lanes remain passes. A skip or zero-test lane is not a pass.

The accepted Stage E v1 records and `final-manifest.json` retain their exact
accepted closed shapes, names, order, semantics and nine-entry v1 manifest.
They are never validated as reconciliation-v2 records. Reconciliation adds
five separately named records:

- `schema-replay-v2.json`;
- `recursive-growth-v2.json`;
- `dag-cache-correction-v2.json`;
- `dynamic-growth-guard-v2.json`;
- `harness-artifact-v2.json`;

and one `reconciliation-manifest-v2.json`. The v2 manifest seals the accepted
v1 manifest plus those five records. A v2 PASS requires the accepted v1
manifest and all five v2 records to be PASS. Any `BOUND_NOT_SUPPORTED` record
produces only `STAGE_E_SCIENTIFIC_HARNESS_BOUND_NOT_SUPPORTED`, refuses every
dependent Stage F route, and can never be relabelled PASS. The non-PASS
manifest retains the same five exact ordered record names, permits each entry
to state PASS or `BOUND_NOT_SUPPORTED`, requires at least one bound entry, and
requires `bound_record_names` to equal exactly the names of the bound entries.

The closed reconciliation evidence schema contains exactly 22 definitions,
seven top-level roots, and 83 local `$ref` occurrences resolving to exactly 22
unique local targets with zero unresolved reference. The same exact profile is
frozen in the mechanical and validation contracts; a missing, extra, renamed,
or unresolved definition or root refuses validation.

The recursive, poset, transport, and correction records retain ordered
canonical per-case identities whose preimages contain exact input operands,
authority, algorithm/oracle identity, output/result and relation disposition.
Aggregate counts or zero-mismatch fields never substitute for those case
identities. The dynamic guard retains exact ordered coverage of all fourteen
study routes plus `SD-01-GROWTH-v1`, and every one must refuse before import.

Stage E evidence is limited to theorem checks, static/synthetic implementation tests, numerical oracle verification, and harness-complexity verification. Every scientific/model/outcome/result/figure/book/release/publication counter remains zero.

## 13. Checkpoint, cache, and continuation

The harness must validate, not execute scientifically, the accepted immutable campaign/run/attempt/checkpoint identity hierarchy. Deterministic seed zero and empty stream/counter state remain valid. Stochastic fixtures bind the exact rule, seed, permitted stream list, ordered counter tuples, canonical tuple-set digest, and terminal rejection rule.

The non-resetting attempt/run/study counters, ordered attempt slices, exact checkpoints, cumulative accounting, process-allocation receipt equality, campaign policies, cache keys, authority invalidation, and terminal dispositions remain unchanged. A new attempt cannot change inputs, algorithms, authority, environment, numerical policy, topology, approximation status, or scientific meaning.

Stage E may test checkpoint/restart using non-evidence fixtures. It may not create or resume a registered scientific campaign.

## 14. Stage F packet gate

After independent Stage E implementation and integration PASS, the controller must present one human-readable campaign packet covering SD-01 through SD-14 and nested `SD-01-GROWTH-v1` in dependency order. The packet must include measured Stage E throughput, projected active and calendar duration, memory, storage, trace bytes, checkpoint slices, campaign envelopes, controls, falsifiers, and the meanings of PASS, FAIL, and `COMPUTATIONALLY_INCONCLUSIVE`.

No Stage F study begins until one explicit user authorization covers the complete campaign. No registered protocol changes after outcomes are visible.

## 15. Independent gates

The sequence is mandatory:

1. independent audit of this exact six-file authority candidate;
2. normal private integration after PASS;
3. separately audited reachability durability implementation and target CI;
4. replacement Stage E harness implementation within the exact 51-path closure;
5. permitted outcome-blind Stage E validation and immutable evidence;
6. independent harness implementation audit;
7. normal private integration after PASS, exact-target CI/evidence, and independent integration audit;
8. one complete Stage F campaign packet and explicit user authorization.

Self-review is not independent. Main merge, tag, release, upload, package-index publication, registered scientific execution, outcome inspection, results, figures, books, publication, and institutional claims remain forbidden by this authority candidate.

## 16. Stop conditions

Refuse and stop before implementation if any accepted authority input is missing or changed, any predecessor identity fails, any path is outside exact closure, any schema fixture or negative case is substituted or skipped, any dynamic-growth requirement is weakened, any algorithm omits a required counter, any cache traversal uses an empty graph for a claimed nonempty control, any scientific route becomes reachable, or any evidence class is promoted.

Ordinary drafting, checker, strict-JSON, hash, count, fixture, path, or test defects are correctable prospectively and are not scientific contradictions.
