# EBP V3.0 Gate 1D-C — macOS Environment Compatibility Addendum

**PROSPECTIVE PRE-EXECUTION OPERATIONAL COMPATIBILITY AMENDMENT.** This
addendum and `v30_gate1dc_macos_environment_compatibility_contract.json` were
frozen before any Gate 1D-C runner invocation, receipt creation, execution-
start control, model transition, result generation, candidate-outcome
inspection, scientific classification, or outcome interpretation. The failed
check that motivated them was an isolated pre-execution environment probe, not
a runner invocation. The sole attempt
`gate1dc-single-authorized-attempt` remains unconsumed. No Gate 1D-C model
state has advanced and no Gate 1D-C result or outcome has existed or been
inspected.

This amendment is prospective only. It resolves one non-scientific macOS
process-entry compatibility fact and freezes the exact normalization required
before a later, separately authorized execution. It does not implement that
normalization and does not authorize execution, finalization, interpretation,
or publication.

## 1. Authority, precedence, and fail-closed boundary

`V3.0_GATE1D_C_OUTCOME_DISCRIMINATION_PROTOCOL.md` and
`v30_gate1dc_outcome_discrimination_plan.json` retain complete precedence over
every hypothesis, parameter, comparison, metric, threshold, tolerance,
predicate, falsifier, positive control, outcome class, interpretation rule,
limitation, and non-claim. This amendment changes no scientific content,
input, behavior, meaning, or semantic rule.

`V3.0_GATE1D_C_EXECUTION_FINALIZATION_ADDENDUM.md` and
`v30_gate1dc_execution_finalization_contract.json` retain precedence over
every operational rule except the exact macOS environment entry,
normalization, runner and finalizer invocations, source-provenance inventory,
and dependent manifest-provenance rows explicitly superseded here. All state,
receipt durability, authorization-consumption, active-lock, retry, cleanup,
publication, abnormal-termination, recovery, failure, finalization,
immutability, and result-commit rules remain unchanged.

Any actual or suspected scientific conflict, any operational conflict outside
that narrow compatibility scope, or any disagreement between this Markdown
and its JSON contract causes fail-closed refusal. The JSON contract is the
mechanical schema and ordering source; this Markdown is its normative human
rendering. Neither source permits selective application.

## 2. Frozen pre-execution platform record

At starting commit `574b0b097b626fada366d8198ae5175b95020b7a` on the frozen
macOS platform, the original seven-variable `env -i` command was tested only
with an isolated Python environment probe that imported no repository or
scientific module. The process exposed exactly the original seven entries plus
one macOS compatibility entry:

```text
__CF_USER_TEXT_ENCODING=0x1F5:0x0:0x0
```

The observed process environment therefore had exactly eight keys. An
explicit eight-key launch supplying that same value produced exactly the same
eight-key entry environment. Deleting only `__CF_USER_TEXT_ENCODING` inside
the isolated probe restored exactly the original seven-key environment.

The implemented runner and finalizer at that commit require the original
seven-key environment exactly. Consequently, the original official macOS
launch would fail its entry-environment check before any receipt or scientific
import. Neither executable was invoked. The result directory and every
registered final or temporary artifact were absent; authorization was not
consumed; no result was generated.

`__CF_USER_TEXT_ENCODING` is fixed non-scientific platform metadata. It is not
a scientific input, parameter, option, source of entropy, observation, result,
or configurable value.

## 3. Exact eight-key process-entry environment

The runner and finalizer must each enter Python with exactly this eight-key
environment, with no absent or additional key and with each value exact:

1. `PATH=/opt/homebrew/bin:/usr/bin:/bin`
2. `LC_ALL=C`
3. `LANG=C`
4. `TZ=UTC`
5. `PYTHONHASHSEED=0`
6. `PYTHONNOUSERSITE=1`
7. `__CF_USER_TEXT_ENCODING=0x1F5:0x0:0x0`
8. `EBU_GATE1DC_EXECUTION_SHA=<authorized_execution_sha>`

The only runtime substitution remains the explicitly user-authorized,
audited, lowercase 40-hex execution SHA. No compatibility variable or value is
operator-selectable.

## 4. Exact normalization sequence

The later runner and finalizer implementation must perform this sequence
exactly:

1. At process entry, before any scientific or project import, require the
   exact eight-key set and the exact fixed values above, except for the single
   authorized execution-SHA substitution.
2. Independently require `__CF_USER_TEXT_ENCODING` to equal exactly
   `0x1F5:0x0:0x0`.
3. Delete only `__CF_USER_TEXT_ENCODING` from the process environment.
4. Require the remaining environment to equal exactly the original seven-key
   allowlist: `PATH`, `LC_ALL`, `LANG`, `TZ`, `PYTHONHASHSEED`,
   `PYTHONNOUSERSITE`, and `EBU_GATE1DC_EXECUTION_SHA`, with their frozen
   values.
5. Perform every Git subprocess, source check, receipt construction,
   scientific import, runner operation, and finalizer operation only after
   successful normalization.
6. Prohibit every scientific or project module and function from reading,
   receiving, copying, hashing as scientific input, or otherwise depending on
   the compatibility variable.
7. Fail closed before receipt creation or any finalizer action on an absent
   key, alternate value, duplicate declaration, injected value, extra key,
   failed deletion, or post-normalization mismatch.

No other key may be removed, added, rewritten, defaulted, inferred, copied, or
normalized. Failure creates no permission to retry after a receipt path exists
and changes none of the original failure or retry rules.

## 5. Amended official invocations

The runner invocation is exactly:

```sh
AUTHORIZED_EXECUTION_SHA='<AUDITED_40_LOWERCASE_HEX_EXECUTION_SHA>'
env -i PATH=/opt/homebrew/bin:/usr/bin:/bin LC_ALL=C LANG=C TZ=UTC PYTHONHASHSEED=0 PYTHONNOUSERSITE=1 __CF_USER_TEXT_ENCODING=0x1F5:0x0:0x0 EBU_GATE1DC_EXECUTION_SHA="${AUTHORIZED_EXECUTION_SHA}" /opt/homebrew/bin/python3 -B -s -X utf8 exp_v30_gate1dc.py
```

The finalizer invocation is exactly:

```sh
AUTHORIZED_EXECUTION_SHA='<THE_SAME_AUDITED_40_LOWERCASE_HEX_EXECUTION_SHA>'
env -i PATH=/opt/homebrew/bin:/usr/bin:/bin LC_ALL=C LANG=C TZ=UTC PYTHONHASHSEED=0 PYTHONNOUSERSITE=1 __CF_USER_TEXT_ENCODING=0x1F5:0x0:0x0 EBU_GATE1DC_EXECUTION_SHA="${AUTHORIZED_EXECUTION_SHA}" /opt/homebrew/bin/python3 -B -s -X utf8 finalize_v30_gate1dc.py
```

No other invocation, variable, value, wrapper, redirection, pipe,
substitution, executable, argument, or normalization is permitted.

## 6. Strict compatibility-contract parsing and hashing

The compatibility JSON must be parsed as UTF-8 without BOM and must reject
duplicate object keys, `NaN`, `Infinity`, `-Infinity`, trailing data,
comments, invalid UTF-8, and every non-JSON token. Every numeric value must be
finite, and a Boolean is not a number where a numeric field is required.

Its canonical bytes are exactly:

```python
json.dumps(value, sort_keys=True, separators=(",", ":"),
           ensure_ascii=True, allow_nan=False).encode("utf-8")
```

with no final newline. A later implementation must freeze and validate both
the raw SHA-256 of the exact committed compatibility-contract bytes and the
SHA-256 of these canonical bytes before any Git subprocess, receipt creation,
scientific import, runner operation, or finalizer action. The compatibility
Markdown is raw-SHA-256 recorded through the receipt source inventory; the
compatibility JSON is both raw-SHA-256 recorded there and separately locked by
its raw and canonical implementation constants.

## 7. Receipt and source-provenance integration

The exact compatibility source filenames are:

1. `V3.0_GATE1D_C_MACOS_ENVIRONMENT_COMPATIBILITY_ADDENDUM.md`
2. `v30_gate1dc_macos_environment_compatibility_contract.json`

The execution receipt retains its original 16 top-level fields in the original
order. No top-level field is added, removed, renamed, reordered, or
reinterpreted. In particular, `contract_raw_sha256` and
`contract_canonical_sha256` continue to mean only the raw and canonical hashes
of `v30_gate1dc_execution_finalization_contract.json`.

Only the receipt's `source_sha256` object is extended from 13 to 15 ordered
raw-SHA-256 entries. The compatibility Markdown and JSON are inserted
immediately after the original execution/finalization JSON contract. Every
value remains the raw SHA-256 of worktree bytes after exact identity with the
Git blob at the authorized execution SHA is proved. The amended order is:

1. `AGENTS.md`
2. `V3.0_GATE1D_C_OUTCOME_DISCRIMINATION_PROTOCOL.md`
3. `v30_gate1dc_outcome_discrimination_plan.json`
4. `V3.0_GATE1D_C_EXECUTION_FINALIZATION_ADDENDUM.md`
5. `v30_gate1dc_execution_finalization_contract.json`
6. `V3.0_GATE1D_C_MACOS_ENVIRONMENT_COMPATIBILITY_ADDENDUM.md`
7. `v30_gate1dc_macos_environment_compatibility_contract.json`
8. `gate1dc_v30.py`
9. `test_v30_gate1dc.py`
10. `exp_v30_gate1dc.py`
11. `finalize_v30_gate1dc.py`
12. `d0_v29.py`
13. `p1c_v29.py`
14. `ebu_quote_v30.py`
15. `service_v30.py`

Receipt serialization, top-level ordering, immutability, publication,
durability, authorization-consumption, and no-retry semantics remain exactly
as frozen in the original execution/finalization sources.

## 8. Manifest provenance integration

The manifest provenance table is extended only from 13 to 15 source rows. For
receipt source-hash indices 0 through 14, it renders index plus one, the exact
path in the amended source order, and the receipt hash at that key. The two
compatibility rows therefore appear as rows 6 and 7, immediately after the
original execution/finalization JSON contract.

No scientific manifest row, runner-summary field, scientific result field,
artifact descriptor, hypothesis, falsifier, positive-control row,
discriminator row, O3 row, outcome-class row, limitation, nonclaim, fixed
wording, section order, or result artifact is added, removed, renamed,
reordered, or reinterpreted.

## 9. Operational mechanics preserved unchanged

Except for the five narrow supersessions named in §1, the original
execution/finalization addendum and contract apply verbatim. In particular,
this amendment does not change:

- the six registered final artifacts or six temporary basenames;
- the 16-field receipt schema or its point of authorization consumption;
- the execution-start control, lock, or scientific reachability boundary;
- the seven-state classifier or any state transition;
- the single authorized attempt, no-rerun rule, cleanup rule, or 17-row
  failure/retry matrix;
- serialization of receipt, start, trace, stdout, summary, or manifest;
- publication order, no-overwrite linking, modes, file or directory `fsync`,
  abnormal-termination residue preservation, or recovery rule;
- runner-output or full-study completion sentinels;
- mechanical finalizer restrictions or validation semantics, except that its
  environment/source checks and dependent provenance rows use this amendment;
- post-execution immutability or the exact six-file result-commit and push
  procedure.

## 10. Future implementation acceptance criteria — not begun

Implementation is a later authorization boundary. It may modify only:

- `exp_v30_gate1dc.py`
- `finalize_v30_gate1dc.py`
- `gate1dc_v30.py`
- `test_v30_gate1dc.py`

That later static-only implementation must:

- implement exact eight-key entry validation in both executables;
- require the fixed compatibility value, delete only that key, and prove the
  exact seven-key post-normalization environment;
- make every Git subprocess, source check, receipt construction, scientific
  import or call, runner operation, and finalizer operation unreachable before
  normalization;
- update both official invocation strings exactly;
- extend receipt/source provenance to the exact 15-source order;
- separately lock and validate the compatibility JSON raw and canonical hashes
  while preserving the original contract-hash field meanings;
- extend finalizer source validation and only the manifest provenance table to
  15 rows;
- add static and synthetic tests for missing, incorrect, duplicated, injected,
  and extra environment values, failed deletion, and post-normalization
  mismatch;
- prove no scientific import or call occurs before normalization and no
  scientific/project code can read the compatibility variable;
- preserve every scientific function, semantic rule, field, value, and
  invariant; and
- run no scientific code, step, trajectory, runner, or finalizer.

The later implementation must be committed and pushed before any new execution
authorization. This freezing stage does not implement it.

## 11. Stage record and nonclaims

This stage creates only this addendum and its JSON contract and changes
`AGENTS.md` only to register their narrow authority. It does not modify the
scientific protocol or plan, the original execution/finalization sources, the
scientific disposition, any runner, finalizer, test, scientific module, or
book-structure file.

No runner or finalizer was invoked. No scientific function, model step,
trajectory, result construction, outcome inspection, classification,
interpretation, receipt, execution-start control, registered artifact, or
result directory occurred. The sole attempt remains unconsumed. The next
possible stage is the separately authorized compatibility implementation; it
has not begun and does not itself authorize execution.
