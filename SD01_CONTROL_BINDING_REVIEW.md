# Independent SD-01 static control-derivation review

Disposition: **INDEPENDENT_STATIC_DERIVATION_REVIEW_PASS — NOT A SCIENTIFIC BINDING PASS**.
Reviewer: independent scientific-audit agent Carver (`sd01_scientific_audit`).
Review scope: the three exact files below, frozen source fidelity, mathematical
derivations, control non-uniqueness and the prohibition on new semantics.

Starting commit: `db448403da2c7c16ae62479e88bb748e377820e1`.
Final reviewed draft commit: `c07da3d43f46771e204d5519cfd7e73a0d2f6d21`.
This subsequent review-only record does not change the reviewed file bytes.

| File | SHA-256 |
|---|---|
| `SD01_CONTROL_BINDING_AUTHORITY_DRAFT.md` | `46c7fb14ea6545be79c86dbd9d576850a34e02c09e0c3049cf8c360a94acf761` |
| `SD01_CONTROL_DERIVATION.md` | `693ea160154b43f86ec4e2e7a6bcade005cc5c9635e99510eda511370f514d0c` |
| `sd01_control_derivation_checks.json` | `53353f6346dbfb21ea7b6311bb538efcd580abf621361920963a9cfdebc72600` |

The reviewer independently reconstructed all 13 exact arithmetic checks,
verified five frozen source files against their committed bytes and hashes,
and checked both document hashes against the check record. The exact demand
table, four candidate-control derivation chains, safe-cell contraction,
Allee maximum, 26-transition full-service impossibility, H1 stock invariance
and ambiguity witnesses passed this static review.

One initial issue was corrected: the draft asserted that rho was dimensionless
without that convention being expressly frozen. The assertion was removed and
the reviewer verified the corrected final bytes. No concrete finding remains
within this static scope.

The final draft preserves the scientific semantics. Stock-set membership,
service shortfall and the overall viability label remain distinct. It neither
substitutes acceptance predicates nor asserts a contradiction that the source
record does not establish. Candidate mappings remain proposals; the precise
missing semantic selectors are stated rather than adopted. Exact-real proofs
are not binary64 conformance evidence or scientific outcomes.

Approval accepts static analysis only. The Stage F SD-01 gap remains open,
and scientific execution is unauthorized. No numerical control selection is
required from the user by this review; no missing rule is supplied silently.

The reviewer performed no edits, adapter imports, model calls, recurrence
iterations, trajectories, outcome inspection or AWS operations. The controller
likewise performed only source inspection, exact arithmetic, document creation,
hashing and local Git work for this increment. No scientific experiment ran.

Scope and completion: exactly four new files comprise this increment—the
three reviewed files and this review. The authority draft states the narrow
approval scope; the derivation contains the requested tables and proofs; the
JSON retains source hashes and exact checks; this file records independent
review. Existing authority, implementation, results, books and AWS-C0 files
remain unchanged. Complete staged diffs were inspected and `git diff --check`
passed. Local commits were authorized and made; no push or publication occurred.

Next possible stage, not begun: a separately authorized resolution of the
proven missing semantic selectors. The current instruction to add no rule or
interpretation precludes treating a chosen completion as already-frozen
science. No complete control binding, sealed packet or execution authorization
has been created.
